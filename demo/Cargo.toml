[package]
name = "smt-demo"
version = "0.1.1"
edition = "2021"
license = "MIT"
repository = "https://github.com/bendiksdaniels/plsfix"
publish = false
description = "Builds the play-around workbook for pls,fix"

[dependencies]
rust_xlsxwriter = "0.93"

[dev-dependencies]
zip = { version = "7.2", default-features = false, features = ["deflate"] }
