# pls,fix

Office.js add-in for financial modelling with two hosts: the Excel task pane
(`taskpane.html`, model tools + Links tab) and the PowerPoint pane (`pptpane.html`, tracked
Excel->PowerPoint links), TypeScript/Vite/Vitest, plus a Rust axum host (`server/`, port
8804) that serves both panes AND the end-to-end encrypted link relay (`/api/links`,
`/api/inbox`, sqlite at `MODELIS_DATA`) to the DB Automatizācijas suite at
`dbautomatizacijas.com/modelis/` (key `modelis`).

- Pane code in `src/`, tests in `src/` + `test/` (fake Office.js host with strict load
  semantics). `npm run check` (all gates), `npm run build`, `npm run validate`. Dev sideload:
  `npm start` (Excel) / `npm run start:ppt` (PowerPoint); `npm stop` / `npm run stop:ppt`.
- `server/`: `cargo test --manifest-path server/Cargo.toml`. Serves `dist/` (env
  `MODELIS_STATIC`), `/healthz`, `/version`; gateway strips the `/modelis/` prefix.
  The public path carries a Cloudflare Access bypass - Office webviews cannot pass
  Access, so nothing readable may ever be served here. The one exception, by design:
  relay blobs under `/api/` are encrypted in the pane (AES-GCM, keys derived from a
  per-link token that exists only inside a deck's tags and the workbook registry); the
  server stores ciphertext and `sha256(authKey)` only, never plaintext or file names.
- Deploys go through the gateway's audited flow (`db deploy modelis`), which builds the
  pane locally (`npm run build`) and the server binary on the VPS. Never hand-copy files
  to production.
- `manifest.xml` (dev, localhost:3000) and `manifest.prod.xml` (prod, server URLs) are
  both generated from `manifest/spec.ts` via `npm run manifest:build` - never edit
  either file by hand. `npm stop` restores the prod manifest into the wef folders
  (`scripts/wef-restore-prod.sh`).
- `README.md` and `ROADMAP.md` are Daniel's hand-curated files: surgical edits only.
- Toolchain: `typescript` in package.json is aliased to `@typescript/typescript6` (the TS team's
  side-by-side shim) because typescript-eslint refuses TS 7; the real TS 7 compiler is
  `@typescript/native` and still provides `tsc`. Agent worktrees nest under `.claude/`, so lint
  ignores use `**/dist/**`-style globs plus `.claude/**` and `.superpowers/**`.
- Versioning: package.json + server crate stay in lockstep; pane footer shows
  vMAJOR.MINOR.PATCH with a 3-digit patch.

State pins: `tasks/AUTORESUME.md` (resume here), `tasks/v1-plan.md`, `tasks/lessons.md`.

## Map

Entry points: `taskpane.html` + `src/main.ts` (Excel pane, web edge), `pptpane.html` + `src/ppt/main.ts` (PowerPoint pane), `server/src/main.rs` (axum host + relay), `manifest/spec.ts` -> `scripts/build-manifests.ts` (both manifests), `src/functions/index.ts` -> `public/functions.js` -> `dist/` (Excel custom functions, own vite pass; the dev server serves it from `public/` too).

Modules (what each owns):
- `src/main.ts`: Excel pane wiring: `dispatch(action)` switch, ribbon/shortcut command table, brand dashboard (persisted to localStorage and to the workbook; boot adopts the workbook palette over the machine one), sheet explorer, boot in `Office.onReady`. Web edge, no business logic.
- `src/ui/`: shared pane pieces without Office.js: toast, guard, tabs, report (error surface), version, time, clipboard, dom (`getElement`, the by-id lookup both panes use).
- `src/excel/`: the Excel Office.js adapter, one file per feature area behind an `index.ts` barrel that keeps the import path `./excel`: `shared.ts` (public types + `parseAddress`), `internal.ts` (private range/fill/chart-shell/host-capability helpers, not barrel-exported), `undo.ts` (one slot, every area of the last action), `areas.ts` (the selection as one range per area - Excel.RangeAreas on ExcelApi 1.9, one block below it - plus the cell cap over all of them), `selection.ts` (presets, number formats, the format eraser) + `format-cycles.ts` (the fill, font, row-style, number and border cycles), `protection.ts` (what the host refuses when a flow writes - a locked cell of a protected sheet, a write cutting a merged cell - and what the pane says instead of office.js's own string; a paint reports a line, an edit throws a staged one), `sizes.ts` (row-height and column-width cycles, outside pls,fix Undo: sizes are sheet state), `formulas.ts`, `paste.ts`, `autocolor.ts`, `audit.ts`, `trace.ts`, `charts.ts`, `chart-place.ts` (where an inserted chart lands: the first block of cells big enough for it whose own used range is empty and which covers no other chart - right of the anchor, below it, below the sheet's used range - two syncs), `tornado.ts` (sensitivity chart from a helper block beside the selection), `reshape.ts` (unpivot onto a new sheet), `workbook.ts`, `brand-store.ts` (brand palette JSON in `workbook.settings["plsfix.brand.v1"]`; the workbook wins over localStorage, which is the default for new workbooks), `links.ts` (tracked-link flows: export a selection as a picture or as a table, export a chart) + `link-anchors.ts` (registry, anchors, batched `resolveSources`: three syncs whatever the link count, render - a picture or a table) + `link-table.ts` (a range read as a `TableCell` grid: one `getCellProperties`, the displayed `text` and each column's width in one sync; Excel's defaults are left out) + `link-record.ts` (relay round trip: push, announce, publish/rollback) + `link-lock.ts` (`exclusive(stage, work)`, the one queue export/push/remove/auto-push serialise the registry read-modify-write on); neither barrel-exported; also `paintbrush.ts`, `setCellProperties`; also `find.ts` (Super Find in four phases and four syncs: defined names + comment threads, used-range extents, the grids, then each comment's cell; `workbook.comments` is gated on ExcelApi 1.10 and reports `commentsSkipped` below it); also `link-watch.ts`, `worksheets.onChanged`, `workbook.settings["PLSFIX_AUTOPUSH"]`; also `link-highlight.ts` (the Links-tab tint over every anchored range, snapshot in `workbook.settings["PLSFIX_LINK_HIGHLIGHT"]`; charts and `#REF!` anchors skipped) over `fill-store.ts` (the fill snapshots one overlay owns plus its persisted copy; `audit.ts` and `link-highlight.ts` hold one each and each refuses to paint while the other holds fills - `requireSoleOwner`, symmetric - neither barrel-exported; `src/main.ts` restores the audit snapshot before the Links tab restores the highlight); also `styles.ts`, `getCellProperties({ style })`; also `share.ts`; also `separators.ts` (reads Excel's decimal and thousands separators, ExcelApi 1.11, for the Brand-tab note).
- `src/styles.css` -> `src/styles/`: the one stylesheet both panes import, split by area and imported in cascade order: `tokens.css` (the `[hidden]` reset first, then the palette - navy, mint, `--accent-ink` for mint as text - the 11/12/13/15/18 type scale, the tight/block/section gaps, the 34px control height and the 16px tick), `shell.css` (masthead, comma mark, status pill, tab strip, the gaps between and inside sections, footer, toast, the 330px and 405px breakpoints), `controls.css` (the button state machine - hover, active, focus ring, disabled - icon buttons, action lists, ticked rows, io rows, fields, chips), `excel.css` (Tools/Workbook/Links: selection card, format grid, segmented strips, trace panel, sheet explorer, the `wl-` link table), `brand.css` (preview table and its `--pv-*` properties, palette rows, logo colours, the settings grid) and `ppt.css` (link toolbar, the deck's link table and which column gives way at which width, badges, inbox, source chooser).
- `src/excel/`: the Excel Office.js adapter, one file per feature area behind an `index.ts` barrel that keeps the import path `./excel`: `shared.ts` (public types + `parseAddress`), `internal.ts` (private range/fill/chart-shell/host-capability helpers, not barrel-exported), `undo.ts` (one slot, every area of the last action), `areas.ts` (the selection as one range per area - Excel.RangeAreas on ExcelApi 1.9, one block below it - plus the cell cap over all of them), `selection.ts` (presets, number formats, the format eraser) + `format-cycles.ts` (the fill, font, row-style, number and border cycles), `protection.ts` (what the host refuses when a flow writes - a locked cell of a protected sheet, a write cutting a merged cell - and what the pane says instead of office.js's own string; a paint reports a line, an edit throws a staged one), `sizes.ts` (row-height and column-width cycles, outside pls,fix Undo: sizes are sheet state), `formulas.ts`, `paste.ts`, `autocolor.ts`, `audit.ts`, `trace.ts`, `charts.ts`, `chart-place.ts` (where an inserted chart lands: the first block of cells big enough for it whose own used range is empty and which covers no other chart - right of the anchor, below it, below the sheet's used range - two syncs), `tornado.ts` (sensitivity chart from a helper block beside the selection), `reshape.ts` (unpivot onto a new sheet), `workbook.ts`, `brand-store.ts` (brand palette JSON in `workbook.settings["plsfix.brand.v1"]`; the workbook wins over localStorage, which is the default for new workbooks), `links.ts` (tracked-link flows) + `link-anchors.ts` (registry, anchors, batched `resolveSources`: three syncs whatever the link count, render) + `link-record.ts` (relay round trip: push, announce, publish/rollback) + `link-lock.ts` (`exclusive(stage, work)`, the one queue export/push/remove/auto-push serialise the registry read-modify-write on); neither barrel-exported; also `paintbrush.ts`, `setCellProperties`; also `find.ts` (Super Find in four phases and four syncs: defined names + comment threads, used-range extents, the grids, then each comment's cell; `workbook.comments` is gated on ExcelApi 1.10 and reports `commentsSkipped` below it); also `link-watch.ts`, `worksheets.onChanged`, `workbook.settings["PLSFIX_AUTOPUSH"]`; also `link-highlight.ts` (the Links-tab tint over every anchored range, snapshot in `workbook.settings["PLSFIX_LINK_HIGHLIGHT"]`; charts and `#REF!` anchors skipped) over `fill-store.ts` (the fill snapshots one overlay owns plus its persisted copy; `audit.ts` and `link-highlight.ts` hold one each and each refuses to paint while the other holds fills - `requireSoleOwner`, symmetric - neither barrel-exported; `src/main.ts` restores the audit snapshot before the Links tab restores the highlight); also `styles.ts`, `getCellProperties({ style })`; also `share.ts`; also `separators.ts` (reads Excel's decimal and thousands separators, ExcelApi 1.11, for the Brand-tab note).
- Pure model modules (typed in/out, no I/O): `settings` (brand palette), `cycles`, `paste` (fill maths, number-format decimals, formula strings), `classify`, `audit`, `chartmath` (bridge, CAGR, tornado ranking), `model`, `reshape` (wide to long), `workbook`, `rounding` (largest-remainder allocation: the parts add up to the rounded total), `numbers` (house number style per language: grouping, decimal point, currency placement, amounts as text); also `paintbrush`, `plsfix.paint.v1`; also `find`; also `styles-audit`; also `share`.
- `src/functions/`: the Excel custom functions `=PLSFIX.ROUND` / `=PLSFIX.ROUNDSUM`, the only code Office loads into the custom-functions runtime. `index.ts` associates them over `src/rounding.ts` (no DOM, no Office.js, no pane imports) and is built to a standalone `public/functions.js` (gitignored build artefact) by `vite.functions.config.ts`, which `npm run build` runs before the pane build and the `predev`/`prestart` hooks run before any dev server, so the same file is copied into `dist/` for production and served at `/functions.js` in a dev sideload; `metadata.ts` is the typed source of `public/functions.json`, written by `scripts/build-functions-metadata.ts` and gated by `npm run functions:check`. The manifest wires both files plus the `PLSFIX` namespace into the Workbook host (`<AllFormFactors>`), reusing the pane's shared runtime.
- `src/link/` (core, pure): `model` (types + codecs; the payload is a union - a picture, or a table of `TableCell`s capped at `TABLE_MAX_ROWS` 60 x `TABLE_MAX_COLS` 20 - plus `payloadBytes`, what a repaint batch weighs), `crypto` (HKDF/AES-GCM, base64url, tokens), `status` (link state rules, slide fitting), `png` (IHDR size), `workspace` (pairing key + host key store), `relay` (fetch client, typed errors), `geometry` (A1 rectangles, range intersection), `debounce` (key debouncer with an injectable clock); also `fetchLinks`, `wire`.
- `src/ppt/`: `host.ts` (the PowerPoint Office.js code every link flow goes through: scan by tags, insert dispatched on the payload kind, refresh in place - one shape or a whole update-all batch in a single sync, pictures only - or by reinsertion, break, go to slide, read the active slide), `tables.ts` (native tables on PowerPointApi 1.8: `addTable` plus the per-cell text, font, fill and alignment writes; a repaint rewrites the cells where the table sits and rebuilds it at the same corner when the grid changed), `placement.ts` (which slide an insert goes on and where: the selected slide, the boxes the other shapes occupy - empty layout placeholders excepted - and `placeInFreeSpace` over a 960x540 slide, margin 36, gap 12), `picture.ts` (the selection-API picture insert, the only route below 1.8), `shapes.ts` (where a shape sits: the shape property list, the PowerPointApi check, walking into groups and addressing a shape through its `groupPath`), `links.ts` (orchestration: list, update, inbox, and the shared `applyBatch` repaint, cut into host batches by `batching.ts`), `batching.ts` (pure: `planBatches` packs keys greedily under a byte budget, `REPAINT_BUDGET_BYTES` = 8 MiB of payload per `PowerPoint.run`, an item past the budget travels alone; `chunk` cuts a list to a route's item ceiling, used by both the status poll and the batch fetch), `revert.ts` ("Revert last update": repaint the ticked rows from the revision below their tag and write that rev back), `change-source.ts` (pure candidate ranking plus the re-point itself: `host.retagLink` then the shared repaint, both driven by a `FoundLink` carrying the new link's tag and token), `chooser.ts` (the "Change source" picker's three buttons and its enabled-with-one-tick rule), `main.ts` (pane boot, state and one guarded handler per button), `views.ts` (DOM renderers: link table, inbox list, candidate list, status and age labels), `actions.ts` (pure: row keys, which rows a button acts on, update detail lines); also `fetch.ts`, `POST /api/links/fetch`, `MAX_FETCH_ITEMS` 200 / `FETCH_BLOB_CAP` 4 MiB per response beside the 8 MiB repaint budget.
- `src/pane/links-tab.ts`: the Excel "Links" tab (export, workbook link list, push, go to source, remove, link key); `installLinksTab` is the only thing `main.ts` adds for it; also `links-toggles.ts`.
- `server/`: `lib.rs` router + cache middleware, `store.rs` sqlite (WAL, auth hash, 2 revs, TTL sweep, `get_link_rev` for one named revision), `relay.rs` `/api` routes (bearer, ETag, body limits, `GET /api/links/{id}?rev=<n>` for the revert path), `main.rs` env + bind + hourly sweeper; also `fetch.rs`, `POST /api/links/fetch`, `deferred`.
- `scripts/`: the gates and generators npm calls: `build-manifests.ts`, `build-functions-metadata.ts`, `check-version.ts` + `version-rules.ts`, and `ux-check.mjs` -> `scripts/ux/` (the pane UX gate: `browser.mjs` finds playwright-core and a Chromium headless shell, `dev-server.mjs` reuses or boots vite, `panes.mjs` + `fixtures.mjs` seed each pane state through the real renderers, `page-probe.js` is the injected in-page measurement, `report.mjs` the wording). `npm run ux:check` walks both panes at 320/360/420/500 in five states and fails on horizontal scroll, a clipped tab strip or label, a short tap target, sub-11px text, a toast over the tabs or an invisible Tab stop.
- `demo/`: Rust generator (`rust_xlsxwriter`) of the play-around workbook `demo/out/pls,fix Demo Model.xlsx` (git-ignored): a DemoCo model whose sheets feed every tool (P&L with two planted errors, bridge table + charts, tornado block, rounding split, wide grid, hidden sheet, a broken name) and a "Start here" checklist; `layout.rs` is the one row/column map, `pen.rs` counts writes and `tests/workbook.rs` reconciles the saved file against that count. `npm run demo` builds it, restores the prod manifest into both wef folders and opens Excel + PowerPoint.
- `test/`: `fakehost.ts` (in-memory Excel host, strict load semantics), `fakeppt/` (PowerPoint fake: `model.ts` the deck, `objects.ts` the office.js objects over it, `tables.ts` `addTable`/`getTable` and the cell writes, `strict.ts` the load rules) + `fakerelay.ts` + `fakepng.ts`, `clock.ts` (virtual `Clock` for the debounced auto-push window), `chart-placement`/`multi-area`/`merged`/`protection`/`tornado`/`universal` `.integration.test.ts` (the universality suites: where a chart lands, every area of a ctrl-clicked selection, merged and empty cells, a protected sheet, and each tool on the selection a modeller actually makes), `ppt.support.ts` (shared `bootPpt`/`seedLink`/`pushAgain`/`memoryStore` for the `ppt.*.integration.test.ts` suites, split by feature so each file stays under 400 lines; `ppt.perf.integration.test.ts` holds the round-trip budgets, counted by `helpers.syncCount()`), `*.integration.test.ts`.

Data flow, link export (v2): selection -> `src/excel/links.ts` (hidden name anchor + registry in `workbook.settings`) -> `Range.getImage`, or `src/excel/link-table.ts` for a table link -> `src/link/model` encodePayload -> `src/link/crypto` seal -> `src/link/relay` putLink -> `server/relay.rs` -> `store.rs` (ciphertext only) -> inbox item sealed with the workspace key. PowerPoint: `src/ppt/links.ts` listInbox -> open -> `src/ppt/host.ts` insertLink (rectangle + picture fill + tags, or `tables.ts` addTable + cell formats + tags), placed in the slide's free space (`placement.ts`); the pane toasts when the object had to overlap. Update: scanLinks (tags, groups walked three levels deep on PowerPointApi 1.8) -> relay status -> `src/ppt/fetch.ts` fetchLinks (one batch for the whole deck; getLink per row only for what came back deferred) -> open -> `refreshLinks` (`fill.setImage` on the shape each `groupPath` leads to, geometry untouched, one `PowerPoint.run` and one sync per 8 MiB of payload (`batching.ts`); a host below 1.8, one that refuses a shape, or a batch holding a table falls back to `refreshLink` per row, which repaints a table cell by cell). Revert: `src/ppt/revert.ts` getLinkRev(tag.rev - 1) -> open -> the same repaint with the older rev in the tag, so the newer one reads as available again. Change source: `src/ppt/change-source.ts` getLink(new token) -> open -> `retagLink` (both tags, geometry untouched) -> the same repaint -> deleteInbox; the old link stays on the relay and nothing in Excel moves.

Invariants: link identity is the shape tags (`PLSFIX_LINK`, `PLSFIX_KEY`) in PowerPoint and the hidden name `PLSFIX_LINK_<id8>` in Excel, never an address or a position; the server holds ciphertext and `sha256(authKey)` only; refresh never writes geometry except height on an aspect change, and a table only when its grid changed and it is rebuilt at the same corner; every Office.js scalar read is preceded by `load()` + `sync()` (the strict fakes throw otherwise); manifests are generated, never hand-edited; `package.json` is the only typed version; hand-curated files (`README.md`, `ROADMAP.md`) get surgical edits only.

Run / test: `npm run demo` (demo workbook + prod manifest sideload + both apps), `npm start` (Excel sideload with the dev manifest), `npm run start:ppt`, `npm stop` (restores the prod manifest into wef), `npm run check` (tsc, eslint, prettier, vitest, cargo test, manifest:check, validate, functions:check, version:check), `npm run ux:check` (both panes at 320/360/420/500, screenshots under `.superpowers/ux`), `MODELIS_DATA=./data cargo run --manifest-path server/Cargo.toml`, deploy only via the gateway (`db deploy modelis`).

Where bugs live (symptom -> file): wrong cell/format written -> `src/excel/<feature>.ts`; a chart landed on the data -> `src/excel/chart-place.ts`; a ctrl-clicked selection refused -> `src/excel/areas.ts`; `PropertyNotLoaded` -> a missing `load()` in that adapter (reproduce in the fake host first); dead pane button -> `src/main.ts` dispatch or the `[hidden]` cascade in `styles.css`; link not found in PowerPoint -> tags, `src/ppt/host.ts` scanLinks; "source missing" -> anchor resolution in `src/excel/links.ts`; relay 403/404 -> key derivation `src/link/crypto.ts` or TTL in `server/store.rs`; wrong picture size -> `src/link/status.ts`; a table inserted on top of something, or in the wrong place -> `src/ppt/placement.ts`; wrong cell format in a deck's table -> `src/excel/link-table.ts` (what was read) then `src/ppt/tables.ts` (what was written); add-in not loading -> `manifest/spec.ts` + `npm run validate`; pane stuck on "Connecting" -> `src/host-ready.ts` (Office.onReady never settles when the custom-functions runtime fails to start, seen on Excel for the web; the host probe boots the pane degraded); Excel crash on `npm start` -> `package.json` `config.dev_server_port` + vite IPv4 (lessons).
Where bugs live (symptom -> file): wrong cell/format written -> `src/excel/<feature>.ts`; a chart landed on the data -> `src/excel/chart-place.ts`; a ctrl-clicked selection refused -> `src/excel/areas.ts`; `PropertyNotLoaded` -> a missing `load()` in that adapter (reproduce in the fake host first); dead pane button -> `src/main.ts` dispatch or the `[hidden]` cascade in `src/styles/tokens.css`; pane scrolls sideways, a tab or a label clipped at a narrow dock -> the `src/styles/` sheet that owns that area, reproduced by `npm run ux:check`; link not found in PowerPoint -> tags, `src/ppt/host.ts` scanLinks; "source missing" -> anchor resolution in `src/excel/links.ts`; relay 403/404 -> key derivation `src/link/crypto.ts` or TTL in `server/store.rs`; wrong picture size -> `src/link/status.ts`; add-in not loading -> `manifest/spec.ts` + `npm run validate`; pane stuck on "Connecting" -> `src/host-ready.ts` (Office.onReady never settles when the custom-functions runtime fails to start, seen on Excel for the web; the host probe boots the pane degraded); Excel crash on `npm start` -> `package.json` `config.dev_server_port` + vite IPv4 (lessons).
