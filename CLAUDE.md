# pls,fix

Office.js add-in for financial modelling with two hosts: the Excel task pane
(`taskpane.html`, model tools + Links tab) and the PowerPoint pane (`pptpane.html`, tracked
Excel->PowerPoint links), TypeScript/Vite/Vitest, plus a Rust axum host (`server/`, port
8804) that serves both panes AND the end-to-end encrypted link relay (`/api/links`,
`/api/inbox`, sqlite at `MODELIS_DATA`) to the DB Automatizācijas suite at
`dbautomatizacijas.com/modelis/` (key `modelis`).

- Pane code in `src/`, tests in `src/` + `test/` (fake Office.js host with strict load
  semantics). `npm run check` (all gates), `npm run build`, `npm run validate`. Dev sideload:
  `npm start` (Excel) / `npm run start:ppt` (PowerPoint); `npm stop` / `npm run stop:ppt`.
- `server/`: `cargo test --manifest-path server/Cargo.toml`. Serves `dist/` (env Since v2.7.5 `lib.rs` builds the router behind `tower_http::timeout::TimeoutLayer` (`app_with_timeout`, `REQUEST_TIMEOUT_SECS` 30, so a stalled connection answers 408 instead of holding a worker; the layer races the response head, not the body stream), `store_inbox.rs` lists the inbox `created_at DESC, rowid DESC` (two same-second exports tie-break to the row inserted last, so "Paste latest linked" takes the newest), and `public/support.html` + `public/privacy.html` (English, static, no script; the store listing's support page with the `#functions` anchor the custom functions' `helpUrl` points at, and what the add-in stores where) are served by Vite's verbatim copy of `public/` into `dist/`; `manifest/xml.ts` writes `<HighResolutionIconUrl>` (`public/assets/icon-64.png`, 64 x 64) right after `<IconUrl>` and `manifest/spec.ts`'s `supportUrl` is that support page.
  `MODELIS_STATIC`), `/healthz`, `/version`; gateway strips the `/modelis/` prefix.
  The public path carries a Cloudflare Access bypass - Office webviews cannot pass
  Access, so nothing readable may ever be served here. The one exception, by design:
  relay blobs under `/api/` are encrypted in the pane (AES-GCM, keys derived from a
  per-link token that exists only inside a deck's tags and the workbook registry); the
  server stores ciphertext and `sha256(authKey)` only, never plaintext or file names.
- Deploys go through the gateway's audited flow (`db deploy modelis`), which builds the
  pane locally (`npm run build`) and the server binary on the VPS. Never hand-copy files
  to production.
- `manifest.xml` (dev, localhost:3000) and `manifest.prod.xml` (prod, server URLs) are
  both generated from `manifest/spec.ts` via `npm run manifest:build` - never edit
  either file by hand. `npm stop` restores the prod manifest into the wef folders
  (`scripts/wef-restore-prod.sh`).
- `README.md` and `ROADMAP.md` are Daniel's hand-curated files: surgical edits only.
- Toolchain: `typescript` in package.json is aliased to `@typescript/typescript6` (the TS team's
  side-by-side shim) because typescript-eslint refuses TS 7; the real TS 7 compiler is
  `@typescript/native` and still provides `tsc`. Agent worktrees nest under `.claude/`, so lint
  ignores use `**/dist/**`-style globs plus `.claude/**` and `.superpowers/**`.
- Versioning: `sh scripts/release.sh [patch|minor]` after every reviewed merge (bump + commit `vX.Y.Z` + annotated tag; refuses a dirty tree); package.json + server crate stay in lockstep; pane footer shows
  vMAJOR.MINOR.PATCH with a 3-digit patch.
- Public repo since 12.09.2026 (MIT, `LICENSE`): the history holds no secrets and no
  third-party material (the UpSlide training dumps were purged from every commit); never commit
  a key, a tenant name, a login or a competitor's document. Every `v*` tag runs
  `.github/workflows/release.yml`: a GitHub Release with `manifest.prod.xml`, the built pane
  the demo workbook and the demo deck, plus the image `ghcr.io/bendiksdaniels/plsfix:<tag>`. Support URL in
  the manifest = the GitHub repo. Guides: `docs/INSTALL.md` (one-line installers per platform, manual sideload, admin deployment),
  `docs/SELF-HOSTING.md` (compose + `/manifest.xml`), `CONTRIBUTING.md`, `SECURITY.md`.

State pins: `tasks/AUTORESUME.md` (resume here), `tasks/launch-next-steps.md` (the path to the AppSource listing, paused 13.09), `tasks/v1-plan.md`, `tasks/lessons.md`.

## Map

Layout: frontend = `src/` (TS panes), `taskpane.html` + `pptpane.html` (Vite entries stay in the root by Vite convention), `public/` -> `dist/` . backend = `server/` (Rust axum host + link relay), `demo/` + `manual/` + `video/` (Rust tools) . deploy = `deploy/` (systemd unit; `install/` = the four one-line installers - `plsfix-install-windows.ps1` registers the manifest under `HKCU\...\WEF\Developer` like Microsoft's tooling, `plsfix-install-mac.command` copies it into both `wef` folders, plus the uninstallers - attached to every release and run as `irm <url> | iex` / `curl <url> | sh`, `PLSFIX_MANIFEST_URL` for a self-hosted manifest; `Dockerfile` + `Dockerfile.dockerignore` + `docker-compose.yml` + `.env.example` for self-hosters, image `ghcr.io/bendiksdaniels/plsfix`), `manifest/` -> `manifest.xml` + `manifest.prod.xml` . docs = `docs/` (`INSTALL.md`, `SELF-HOSTING.md`, `appsource/` = the store submission kit: listing copy, test notes, privacy policy, checklist, specs, research), `ROADMAP.md`; root `CONTRIBUTING.md`, `SECURITY.md`, `LICENSE` . tasks = `tasks/` . tests = `test/` + `*.test.ts` beside the code. Launch week: no restructuring.

Entry points: `taskpane.html` + `src/main.ts` (Excel pane, web edge), `pptpane.html` + `src/ppt/main.ts` (PowerPoint pane), `server/src/main.rs` (axum host + relay), `manifest/spec.ts` -> `scripts/build-manifests.ts` (both manifests), `src/functions/index.ts` -> `public/functions.js` -> `dist/` (Excel custom functions, own vite pass; the dev server serves it from `public/` too), `public/shortcuts.html` (the printable keyboard-shortcut card, opened from the Excel pane in an Office dialog), `scripts/build-ribbon-icons.ts` -> `public/assets/ribbon/<name>-{16,32,80}.png` (the 32 ribbon command icons - 18 Excel, 14 PowerPoint - drawn as inline SVG and rasterised through `sips`; each button in `manifest/spec.ts` names its `icon`, committed PNGs, `npm run ribbon-icons:build` only when the artwork changes).

Modules (what each owns):
- `src/main.ts`: the Excel pane's web edge: the top-level boot sequence (error reporting installed first, settings/paint-slot load, initial renders), `restoreOverlayFills`, `boot()` (wires every tab's buttons and shortcuts through the shared guard and `src/pane/dispatch.ts`, adopts the workbook brand/separators, installs the Links tab), `probeExcelHost` + the `host-ready`/`Office.onReady` boot; since 09.09 `wireControls` wires every control before any host is known, `registerCommands` runs at module top level, and the probe reads the active worksheet. No business logic; each tab's own state and actions live under `src/pane/`. `installShortcutsPanel(document, onCard)` is installed beside `installFirstRun`, the card button running the same `dispatch("shortcut-card")` dialog (v2.7.7).
- `src/ui/`: shared pane pieces without Office.js: toast, guard, tabs, report (error surface; since v2.8.20 `describeError(error, ctx, action)` prefixes the running action's label - "comps-stats" -> "Comps stats: The sync failed." - ONLY on an office.js error, one carrying a string `code`, whose message has no ": " and does not open with the label's first word; the pane's own sentences travel unchanged), version, time, clipboard, dom (`getElement`, the by-id lookup both panes use); also `help.ts` (`installHelp(document)`, called once per pane after the tabs: hangs a round "?" in every section heading and a card under it, one open at a time, Escape closes, nothing persisted; labels are read off the buttons, sentences come from `src/help/`); also `first-run.ts` (`installFirstRun(root, key, sectionId, onShortcuts?)`, the dismissible "New here?" card shown once per machine off `localStorage`, try/catch around every read and write so a webview that refuses storage just shows the card again). `tabs.ts`: the active tab is the strip's only Tab stop (`tabindex` 0, the others -1, the ARIA tabs pattern) and an arrow, Home or End clicks the tab it moves to, so a click hook such as the Excel pane's `refreshSheets` fires for a keyboard user too (v2.7.3). v2.7.13: `guard.ts` also the pane's re-entrancy latch, on only when `GuardDeps.busyMessage` is set (the PowerPoint pane; the Excel pane passes nothing and is unchanged), so a second flow mid-batch is answered, never interleaved. `confirm.ts` (23.09, v2.8.23): the one two-click confirm (`armConfirm`: first press relabels the button's own `<strong>`, or an icon button's title + aria-label, to "Click again to confirm" for 5 s, the second press runs; `when` skips the window, `label` reads a live resting label) behind every destructive or one-way button in both panes: Delete unused styles, Delete broken names, Clean past the data and Reset all (`data-confirm` in `taskpane.html`, wired by `wireControls`), Remove link, Generate when a key exists, Forget key, Break link and Reset brand. `clipboard-links.ts` (local links, S1): `beginClipboardWrite` (the synchronous `navigator.clipboard.write`, `text/html` holding the bundle under `data-plsfix-links`, `text/plain` the one-line fallback sentence, started inside the press per WebKit), `copyBundleNow` (the `execCommand("copy")` fallback behind the Copy for PowerPoint button) and `bundleFromPaste` (reads a paste's `text/html` first, then `text/plain`).
- `src/styles.css` -> `src/styles/`: the one stylesheet both panes import, split by area and imported in cascade order: `tokens.css` (the `[hidden]` reset first, then the palette - navy, mint, `--accent-ink` for mint as text - the 11/12/13/15/18 type scale, the tight/block/section gaps, the 34px control height and the 16px tick; since v2.5.0 the shell owns scrolling - `html, body` overflow hidden, `.app-shell` scrolls vertically - so nothing can push the docked pane sideways; `.sr-only` for labels only screen readers get), `shell.css` (masthead, comma mark, status pill, tab strip, the gaps between and inside sections, footer, toast, the 330px and 405px breakpoints), `controls.css` (the button state machine - hover, active, focus ring, disabled - icon buttons, action lists, ticked rows, io rows, fields, chips), `search.css` (the "Find a tool" input and its results list, a card that pushes the tab panels down rather than floating over them), `excel.css` (Tools/Workbook/Links: selection card, format grid, segmented strips, trace panel, sheet explorer, the `wl-` link table), `brand.css` (preview table and its `--pv-*` properties, palette rows, logo colours, the settings grid), `ppt.css` (link toolbar, the four-control filter grid, the deck's link table - its folder header rows, the workbook line under every object, the widths of its five columns at 460px; no column gives way since v2.8.18 - badges, inbox, the "Paste latest linked" button, source chooser, the Tools tab's action rows and button grids) and `help.css` (the "?" toggle - a 20px disc inside a 34px box, because the UX gate refuses a button under 32px - and the help card).
- `src/excel/`: the Excel Office.js adapter, one file per feature area behind an `index.ts` barrel that keeps the import path `./excel`: `shared.ts` (public types + `parseAddress`), `internal.ts` (private range/fill/chart-shell/host-capability helpers, not barrel-exported); `overCap` and `pickScannableSheets` treat a negative `cellCount` as over the cap; the chart surface guards `roundedCorners` on 1.9, `undo.ts` (five slots, newest first, 25 000 cells across them; since v2.8.20 a capture is PENDING until the write lands: `syncWrite`/`paintSync` call `commitUndo()` on success and `discardUndo()` on a refusal, a new capture drops a stale pending entry, `undoLastAction` never restores one, so a refused write spends no slot) (since v2.8.15 the restore runs every captured cell through `settableProperties`: Excel for Mac reads an unfilled cell back as `{pattern: null, patternColor: ""}` and refuses that shape on write, so the fill is rebuilt as `{pattern: "None"}` or the pattern plus only the non-empty colours, the font key by key, each border edge with only its non-empty fields, no `@odata.type` key survives) + `undo-stack.ts` (pure ring: depth and cell-budget eviction), `areas.ts` (the selection as one range per area - Excel.RangeAreas on ExcelApi 1.9, one block below it - plus the cell cap over all of them), `selection.ts` (presets, number formats, the format eraser) + `format-cycles.ts` (the fill, font, row-style, number and border cycles), `protection.ts` (what the host refuses when a flow writes - a locked cell of a protected sheet, a write cutting a merged cell - and what the pane says instead of office.js's own string; a paint reports a line, an edit throws a staged one; since v2.8.20 `protectedNote(stage, areas)` and `syncWrite(context, stage, refused, areas)`: a multi-area batch - the hygiene cycles, presets, number formats, the paste suite pass their area count - is worded "partly protected, the areas before the locked one were changed, the rest were not", never "nothing was changed"); since 09.09 every writing flow in this folder ends on it, `sizes.ts` (row-height and column-width cycles, outside pls,fix Undo: sizes are sheet state), `formulas.ts`, `paste.ts`, `autocolor.ts`, `audit.ts`, `trace.ts`, `charts.ts`, `chart-place.ts` (where an inserted chart lands: the first block of cells big enough for it whose own used range is empty and which covers no other chart - right of the anchor, below it, below the sheet's used range - two syncs); a null row height or column width (a non-uniform anchor) falls back to Excel's 15 pt / 48 pt, `tornado.ts` (sensitivity chart from a helper block beside the selection), `reshape.ts` (unpivot onto a new sheet), `workbook.ts`, `brand-store.ts` (brand palette JSON in `workbook.settings["plsfix.brand.v1"]`; the workbook wins over localStorage, which is the default for new workbooks), `links.ts` (tracked-link flows: chart export, list, push, go to source, remove); a 413 on a push says `TOO_BIG_TO_SEND` + `link-export.ts` (the selection exports - `exportSelection`, `exportSelectionAsTable`, `exportSelectionAsText` - and the rules that refuse one before anything is anchored) + `link-anchors.ts` (registry, anchors, batched `resolveSources`: three syncs whatever the link count) + `link-render.ts` (what a source becomes: picture, table grid or the range's text); `requireImageApi` gates pushes as well as exports on ExcelApi 1.9 + `link-record.ts` (relay round trip: push, announce, publish/rollback) + `link-touch.ts` (boot-time TTL refresh: registry -> `touchLinks`) + `link-lock.ts` (`exclusive(stage, work)`, the one queue export/push/remove/auto-push and, since v2.8.20, `link-projects.ts`'s `setActiveProject` / `moveLinksToProject` serialise the registry read-modify-write on); neither barrel-exported; also `paintbrush.ts`, `setCellProperties`; also `find.ts` (Super Find in four phases and four syncs: defined names + comment threads, used-range extents, the grids, then each comment's cell; `workbook.comments` is gated on ExcelApi 1.10 and reports `commentsSkipped` below it); also `link-watch.ts`, `worksheets.onChanged`, `workbook.settings["PLSFIX_AUTOPUSH"]`; also `link-list-watch.ts` (`watchWorksheetEdits`: one more workbook-wide `onChanged` handler, debounced at `LIST_WATCH_DELAY_MS` 2 000 ms, for any caller that just wants to know the workbook went quiet after an edit - no on/off switch of its own, unlike auto-push); also `link-highlight.ts` (the Links-tab tint over every anchored range, snapshot in `workbook.settings["PLSFIX_LINK_HIGHLIGHT"]`; charts and `#REF!` anchors skipped); a refused paint hands the fills back over `fill-store.ts` (the fill snapshots one overlay owns plus its persisted copy; since v2.8.13 `originalFillColor(sheetId, row, column)` answers the colour a painted store remembers under a cell, so `link-table.ts` ships the model's own fill, never an overlay's tint; `audit.ts` and `link-highlight.ts` hold one each and each refuses to paint while the other holds fills - `requireSoleOwner`, symmetric - neither barrel-exported; `src/main.ts` restores the audit snapshot before the Links tab restores the highlight); also `styles.ts`, `getCellProperties({ style })`; also `share.ts`; also `model-check.ts` (`runModelCheck`, read-only: three syncs, one grid load per sheet under `MODEL_CHECK_CELL_CAP` 200 000, hidden sheets read but never activated, names and styles through the two scrubbers' own scans); also `separators.ts` (reads Excel's decimal and thousands separators, ExcelApi 1.11, for the Brand-tab note); also `reconcile.ts` (`reconcileSelection(target, tolerance)`: one selected block, its finite numeric cells through `solveReconciliation`, then `worksheet.getRanges(...).select()` so the answer is the selection - five syncs: the area count, the `withinCap` cell count before any value is read, the values, the matched addresses, the select); `paintbrush.ts` also owns `loadWorkbookPaintSlots` / `saveWorkbookPaintSlots` (`workbook.settings["PLSFIX_PAINT_SLOTS_V1"]`: the workbook wins over localStorage, a null read means this workbook never saved slots and the machine's stay; the save never throws and answers whether the workbook copy landed, so a host refusing settings still keeps the capture in memory and localStorage). Since v2.7: `stats.ts` (`insertCompsStats`: the six-row block of live formulas - MIN, PERCENTILE.INC 0.25, MEDIAN, AVERAGE, PERCENTILE.INC 0.75, MAX - one blank row under a selected comps table, one column of numbers at a time, labels in the first column; the cap, the six-empty-rows refusal, Undo capture, formulas then formats on `syncWrite`), `football.ts` (`insertFootballField`: the valuation-range chart - a `Method | Low | Range` helper block beside the selection and a `barStacked` chart over it with the floor series cleared, `FOOTBALL_ROW_CAP` 20, `reversePlotOrder` on ExcelApi 1.7 so row 1 sits on top, the value axis in the low column's format on 1.8, a row entered high first swapped and counted), `pinstripes.ts` (`applyPinstripes(axis)`: every second row or column of the selection banded in the palette's primary at 10 % over white (`BAND_TINT` 0.9 through `tint`), cleared again on a second press when every banded cell still carries exactly that key; refuses while `auditOverlayOn()` or `linkHighlightOn()` owns fills; `paintSync`; since v2.8.20 a row band under an active AutoFilter that covers the selection (`filterHiddenRows`: `worksheet.autoFilter` on ExcelApi 1.9, `enabled` + `isDataFiltered` + `getRange()` intersecting) bands every second VISIBLE row like Excel's own banded table, a hand-hidden row outside a filter still counts, columns always by position). Hygiene tools (v2.7): `format-cycles.ts` also cycles indent, alignment and underline on one private `applyHygieneCycle` shape (read one scalar off the active cell, step the ladder in `src/cycles.ts`, write into every area, Undo capture, `syncWrite`); `workbook.ts` also the sheet tools `setSheetsVisibility` (unhide all, very hidden only when asked), `showOnlySheet`, `burySheet` (never the last visible sheet) and `moveSheet` (up/down/end through `worksheet.position`; since v2.8.20 the receipt's position is `visibleRank`, the tab's place among VISIBLE sheets after the move), all asking `structureProtected` first and ending on `syncWrite(..., structureNote)`, outside pls,fix Undo like `sizes.ts`; `protection.ts` also the workbook-structure case (`structureProtected` on ExcelApi 1.7 `WorkbookProtection`, `structureNote`, `syncWrite`'s optional third argument); `clean.ts` (`cleanPastData`: the sheet's used range against the one its values fill; the rows below and the columns right of the data are deleted, or only stripped of formatting when the sheet holds a chart or a shape, or when a host below ExcelApi 1.4/1.9 cannot count them (`Drawings.known`, fail closed); one sync to read, one to write; outside Undo). Audit tools (v2.7.6): `consistent.ts` (`selectConsistentRegion`, read-only in four syncs: the area count, the current region around the active cell (`getSurroundingRegion`) counted against `SELECTION_CELL_CAP` before any grid is loaded, that one grid, then `getRangeByIndexes(...).select()`; a select is not a write, so it ends on a plain sync); `audit.ts` also `scanCapSentence(tool)`, the one wording of the audit tools' scan cap, so the overlay and the region tool each name themselves; `trace.ts` also `queueTrace` (the ExcelApi 1.12 guard plus the queued call, so several cells ride one batch) and `traceAreas`, shared by `traceActiveCell` and `trace-many.ts` (`tracePrecedentsOfSelection`: the Smart Track precedents step over every formula cell of one block up to `MULTI_TRACE_CELL_CAP` 50, five syncs, a batch that hits ItemNotFound halved and asked again, cell by cell once down to `FLAT_RETRY_CELLS` 4; only the same-sheet part of the union is selected, the rest listed). Paste suite additions (v2.7.8) in `paste.ts`: `pasteDuplicateFormulas` (source shape at each area's top-left, the offset and both sheet names through `duplicateFormula`, a destination overlapping the block refused, Undo captured, `syncWrite`), `pasteNumberFormats` (the source `numberFormat` grid tiled over the selection, grown only when the selection is smaller in BOTH axes, the destination counted through `overCap` before tiling) and `pasteRowHeights` (per-row `format.rowHeight` writes because a band write sets every row alike, hidden source rows skipped and counted, `ROW_HEIGHT_ROW_CAP` 500, outside pls,fix Undo like `sizes.ts`). Excel follow-ups (v2.7.9): `formulas.ts` `fastFillAuto` counts its computed extent against `SELECTION_CELL_CAP` before `captureUndo` or the write ("Fast fill supports up to 5,000 cells at once."); `internal.ts` `SHEET_SCAN_CELL_CAP` 200 000 is the one cap Find, prepare-for-sharing and the model check scan under (every scanned sheet's grid loads in one sync; `MODEL_CHECK_CELL_CAP` aliases it), and `loadSheetNames` + `brokenEverywhere` are the shared readers of workbook AND sheet-scoped names (`Worksheet.names`, ExcelApi 1.4) that the scrubber (`Sheet!Name`), prepare-for-sharing and the model check all use; `find.ts` / `share.ts` result lines say how many sheets were skipped and over what cap ("Searched 8 sheets, 1 skipped over 200,000 cells: Notes."); `workbook.ts` `deleteBrokenNames` deletes from whichever collection holds the name and answers a sentence when the sync is refused; `charts.ts` `formatSelectedChart` puts pie leader lines on at the series level too (`ChartSeries.showLeaderLines`, ExcelApi 1.9, in the tolerated surface batch: `applySeriesLeaderLines`) so desktop 365 below 1.19 keeps them. Since v2.7.10: `chart-blocks.ts` (the helper block the tornado and the football field share: `readTriples` with per-chart `TripleRules` - header detector, minimum, optional row cap, refusal wording - `valueFormat` off the first data row's low cell, `requireRoomBeside` against `SHEET_COLUMNS`, and `writeHelperBlock`, which lands the three columns right of the selection under `requireEmptyBlock` -> `sheetProtected` -> `captureUndo` -> `syncWrite`, so a refusal never spends an Undo slot; since v2.8.20 also `serialised(work)`, the one promise queue `insertFootballField` and `insertTornado` run through, so a double press meets the first press's block and its "not empty" refusal instead of drawing twice; not barrel-exported); `fill-store.ts` gained `requireNoOverlayOwner(stage, except?)`, the module-level refusal both overlays and the pinstripes go through (`requireSoleOwner` is the overlay's own call with itself excepted; the sentence comes from the owning store's `label`); `stats.ts` checks protection before the Undo capture and writes a number format per statistics column only, so label and text columns keep their formatting; `football.ts` (`FOOTBALL_ROW_CAP` 20, `reversePlotOrder` on ExcelApi 1.7) and `tornado.ts` share the block write and the room check in `chart-blocks.ts`; `pinstripes.ts` refuses while any fill store is painted, naming the owner. v2.7.11 (P2): `sizes.ts` passes "Row height" / "Column width" as the stage to `selectedSingleRange` and `syncWrite` because those strings ARE the toast ("Row height: select a single range").
- Pure model modules (typed in/out, no I/O): `settings` (brand palette), `cycles`, `paste` (fill maths, number-format decimals, formula strings), `classify`, `audit`, `chartmath` (bridge, CAGR, tornado ranking), `model`, `reshape` (wide to long), `workbook`, `rounding` (largest-remainder allocation: the parts add up to the rounded total), `numbers` (house number style per language: grouping, decimal point, currency placement, amounts as text); also `localizeNumberText(text, separators)` (v2.8.13: Office.js renders a number's text with the invariant comma and point whatever Excel shows, so a table export rewrites a plain-number text to the application's `decimalSeparator` / `thousandsSeparator` (ExcelApi 1.11, loaded in the same batch) in one pass; a currency or date text is not that shape and passes through); `separatorsMatch` reads U+00A0 and its cousins as the house space; also `chart-labels` (where a chart's value labels sit, by chart type), `chart-colors` (the brand colour rules the sheet and the slide share: series palette, waterfall totals/rises/falls, pie cycle), `chart-shapes` + `chart-shapes-parts` (a chart's primitives for a slide - bars, floating waterfall bars and connectors, pie wedges from 12 o'clock, labels, legend - from `ChartData` and a box) + `chart-shapes-line` (the line kind: one ellipse marker per point at the centre of its category slot, a connector to the point before built by `lineBetween` - box always non-negative, `rising` set when the segment climbs left to right, because PowerPoint refuses a negative side and has no flip - the value label above the marker or below it when the marker sits on the plot's top edge; every primitive inside the box); also `paintbrush`, `plsfix.paint.v1`; also `find`; also `styles-audit`; also `share`; also `model-check` (the reviewer's pass: the eight finding kinds, their order, the 500 cap, the toast line and the copyable report); also `tool-search` (`rankTools`: an AND filter over query words against a label and a help sentence, label hits weighted over sentence hits, a bonus for a whole-label prefix, ties kept in input order); also `reconcile` (`solveReconciliation`: meet-in-the-middle subset sum over at most `RECONCILE_MAX_VALUES` 34 finite values, the closest sum within the tolerance and the fewest cells on a tie, `null` when nothing reconciles; `reconcileSummary` is the pane's result line in the house number style of the Brand language). Also `stats` (the comps block: header flag, data-row span, numeric-column offsets, the six formula rows and their preset grid; A1 spans via `cellAddress` in `find.ts`); `chartmath` also `footballField` (each row's low, high and the band between them, a backwards row swapped with a flag, selection order kept). `cycles` also the three hygiene ladders (`INDENT_CYCLE` 0/1/2/3, `ALIGN_CYCLE` Left/Center/Right/General, `UNDERLINE_CYCLE` Single/Double/None) with their canonicalisers (Excel answers `SingleAccountant` and `General`); the file stands at 398 lines: the next addition splits it. `chart-shapes-parts` `barLabel` places a horizontal bar's value label past the bar end where the chart box has room and inside the bar end (white ink on a dark fill) where it has not, clamped to the chart's four edges; `chart-shapes` `barBars` bands a stacked bar chart's series into one row per category like `columnBars` (v2.7.3; both files sit at 398-399 lines: the next change splits the bar family out). `audit` also `consistentRegion(grid, row, column)`: the rectangle of identical R1C1 formulas grown around a cell, horizontal before vertical to a fixed point (greedy, not the largest), `null` when the cell holds no formula. Since v2.8.10 `auditGrid` marks a typed number whose across neighbours are formulas (equal ones when both exist) as `typed`, painted as one solid tint by `src/excel/audit.ts`, and a formula compares with the NEAREST formula across, stepping over typed numbers (never over blanks or text), so the formula beside a hardcode is no longer `lone`; `classify.hasHardcodedNumber` strips the identity constants 0 and 1 (`IDENTITY_CONSTANTS`, whole numeric tokens only) before its digit test, so `=A1-1` and `=A1*(1+B1)` read as plain formulas while `=A1/12` stays partial. v2.8.12: `classifyCell` answers `text` for a non-formula string (trimmed non-empty), which Autocolor leaves uncoloured (`classFont` null): labels and units keep their own colour, blue stays for typed numbers. `shortcuts-model` (v2.7.7, the shortcut manager's grammar: `public/shortcuts.json` read into `{id, name, defaultKey}` rows in the card's own order; `parseCombo` against Microsoft's documented rules, one or more of Ctrl/Alt/Shift/Cmd plus exactly one key from A-Z, 0-9, `-`, `_`, `+` or an arrow, Shift never alone, `Option` folded to Alt (the one mapping the docs state for macOS), Cmd kept as its own modifier, no Cmd/Option relabel on display; `diffShortcuts` building the `{[actionId]: string | null}` map `replaceShortcuts` takes with null = back to the shipped default; `firstClash` comparing EFFECTIVE keys, custom where set, default otherwise, so a custom landing on another action's shipped default is named). `formula-refs` (v2.7.8: every A1 reference in a formula string: `findReferences` reads a token's sheet prefix (quoted, unquoted or `[Book]`-prefixed), its corners and their `$` markers, ignoring text in double quotes, structured-reference bracket bodies, function names a `(` runs into and defined names; `formatReference` writes a token back with new corners and, when asked, a new prefix; `sheetPrefix` quotes a sheet name where Excel would (apostrophes doubled, address-like names quoted); `rewriteReferences` splices only the tokens a caller answers for; `GRID_ROWS` 1 048 576 / `GRID_COLUMNS` 16 384 are its own), `formula-duplicate` (`duplicateFormula(formula, block, offset, toSheet)`: a reference wholly inside the copied block moves with it whatever its `$` markers say; one outside keeps the cells it was written for, which on a paste onto ANOTHER sheet means gaining the source sheet's prefix, while an in-block reference written with the source sheet's name is re-pointed at the destination; a prefix naming any other sheet is outside by definition; a straddling range never moves), `paste` also `tileGrid` (a source grid repeated over a larger target the way `Range.copyFrom` does, last tile cut short). Also `layout` (v2.8, PowerPoint insert placement: `spotBox` picks the rectangle for an `InsertTarget`'s `Where` value over the 960x540 slide, `fitInto` centres an object inside a box keeping its aspect; `src/ppt/placement.ts`'s `resolveTarget` is the one caller). Since v2.8.14 `free-space` (split out of `layout`: `placeInFreeSpace` never shrinks an object below the caller's `minScale`; past that it is centred full size, `overlapping: true`, with the largest hole's size in `freeSpotSize` so the toast can say "the largest free spot is 888 x 40 pt"; charts pass `minPlacementScale`, a picture `PICTURE_MIN_SCALE` 0.5, a text link 1), `text-extent` (`textOccupiedHeight`: the lines a placeholder's or text box's text needs at its font size, PowerPoint's insets included, never taller than the frame; `placement.ts` `occupiedBoxes` trims a Placeholder or TextBox to it after one guarded sync that loads text and size for those two types only) and `chart-shapes-bar` (the bar family split out of `chart-shapes` for the line cap). `chart-shapes` v2.8.14: `LABEL_PAD` 15 and `CHAR_WIDTH` 0.6 cover PowerPoint's text-box insets; a column chart whose slot is narrower than its widest value label draws no value labels at all, and the shared `categoryLabels` keeps every k-th category (the first always) once a slot is narrower than the widest label (bars judge by row height), instead of truncating each to an ellipsis. `cycle-labels` (23.09): the human name of every cycle step ("1,234.0", "Header rule", "heavy underline"), in the same order as the cycle tables, so each format cycle's receipt says where it landed ("Number format: 0.0x", "Row style: Filled", "Fill: Accent", "Borders: box", "Indent: 2", "Aligned: centre", "Underline: double"); Row height / Column width say the size and "outside pls,fix Undo", IFERROR "added to N formulas" / "stripped from N", CAGR "written at Sheet!Cell" (`test/stress.hygiene.receipts.integration.test.ts` walks every lap).
- Templates (the ready calculation blocks the Tools tab's second section, right under the Selection inspector, writes as a 2x3 grid): `src/template-cells.ts` (pure: cell kinds, number formats, the `{r+dr,c+dc}` placeholder grammar read from the block's own top-left corner, the cell constructors) -> `src/template-blocks.ts` (pure: the six grids - debt schedule, DCF, NPV/IRR, working-capital days, a two-way sensitivity grid, an EBITDA bridge shaped for `insertWaterfall`) -> `src/templates.ts` (the catalogue and the one public entry point) -> `src/excel/templates.ts` (`insertTemplate(id)`: refuses a block that is not empty, captures Undo, writes values and formulas in one batch and number formats plus presets in a second, then selects it). `src/excel/presets.ts` owns the five brand presets plus the plain label look, and is the one place `applyPreset` and a template block both paint through.
- `src/functions/`: the Excel custom functions `=PLSFIX.ROUND` / `=PLSFIX.ROUNDSUM`, the only code Office loads into the custom-functions runtime. `index.ts` associates them over `src/rounding.ts` (no DOM, no Office.js, no pane imports) and is built to a standalone `public/functions.js` (gitignored build artefact) by `vite.functions.config.ts`, which `npm run build` runs before the pane build and the `predev`/`prestart` hooks run before any dev server, so the same file is copied into `dist/` for production and served at `/functions.js` in a dev sideload; `metadata.ts` is the typed source of `public/functions.json`, written by `scripts/build-functions-metadata.ts` and gated by `npm run functions:check`. The manifest wires both files plus the `PLSFIX` namespace into the Workbook host (`<AllFormFactors>`), reusing the pane's shared runtime. Because the functions live on the SHARED runtime, `taskpane.html` itself loads `./functions.js` right after office.js (since v2.8.17; the extension point's `<Script><SourceLocation>` is ignored on a shared runtime, and without the tag every `=PLSFIX.*` answered #VALUE! on the desktop from v1.1 to v2.8.16); the `associate` calls are guarded by `typeof CustomFunctions` so the UX gates' hostless browser does not throw; `scripts/gates.test.ts` pins the tag (and its absence from `pptpane.html`). Excel STORES a custom-function formula as `_xldudf_PLSFIX_CAGR(...)`: the demo writes that form (`demo/src/sheets/cagr.rs`), `share.ts` `isAddinFormula` matches both spellings and `model-check.ts` strips the prefix before judging the function name. Since v2.7 the functions are `=PLSFIX.ROUND`, `=PLSFIX.ROUNDSUM` and `=PLSFIX.CAGR(first, last, periods)` (`smtCagr` over the pure `cagr` in `src/chartmath.ts`; a non-positive start, end or period count raises the same `invalidValue` error as the rounding functions), each with a `helpUrl` (`HELP_URL` in `metadata.ts` -> `/modelis/support.html#functions`). v2.7.11 (P2): `smtCagr` refuses a rate that is not finite with the same `#VALUE!` sentence as every other bad argument; `chartmath.cagr` takes the root in log space once `last / first` overflows a double, so only a rate no double can hold is refused.
- `src/link/` (core, pure): `model` (types + codecs; the payload is a union - a picture, a table of `TableCell`s capped at `TABLE_MAX_ROWS` 60 x `TABLE_MAX_COLS` 20 (an optional `h`, true when row 0 is a header), or a text (one cell's displayed text, `TEXT_MAX_CHARS` 500) - plus an optional `chart` on a picture payload, or `chartIssue`, why Excel shipped none, and `payloadBytes`, what a repaint batch weighs), `paint-map` (since 16.09: `encodePaintMap`/`decodePaintMap`, the `PLSFIX_PAINT` tag's own wire format for which cells a table link's own repaint filled - `"1;0:0-2,5;2:3"`, version then one `row:ranges` token per row that carries a fill, `"1;"` alone meaning none; `null` for absent, malformed or a foreign version, which `src/ppt/table-style.ts` reads as a legacy deck; `paintKey`), `chart-model` (`ChartData`: the kinds a slide can draw, the caps - 40 points, 6 series, 12 slices - the validator, `chartCapIssue` + `seriesCountIssue` + `pictureNote`, the one wording of every "as a picture" reason, `normalizeAngle`), `crypto` (HKDF/AES-GCM, base64url, tokens), `status` (link state rules, slide fitting), `png` (IHDR size), `workspace` (pairing key + host key store), `relay` (fetch client; `touchLinks` chunks a workbook at 200 and sums the counts) + `relay-error` (the typed `RelayError` and the status-to-kind map), `geometry` (A1 rectangles, range intersection), `debounce` (key debouncer with an injectable clock), `project` (the project name rule - `cleanProject`, `PROJECT_MAX` 40, `projectLabel` - and since v2.8.18 `groupByProject`, the one grouping rule the Excel Links list, the PowerPoint Inbox and the deck list share: named projects in locale order, No project last, input order kept inside a group); also `fetchLinks`, `wire`. `chart-guard` (`guardChart`, run by `decodePayload` before the payload validator: a `chart` the `ChartData` validator refuses is dropped and noted as `chartIssue` "chart data unreadable", the payload's own reason wins, so a good picture is never lost with a bad chart; `model.ts` no longer validates `chart` itself). `relay-timeout` (v2.7.5): the deadline every relay round trip runs under, `RELAY_TIMEOUT_MS` 20 000 through a manual `AbortController` + `setTimeout` (never `AbortSignal.timeout`, missing on old webviews), raced against the fetch, the timer cleared in `finally`; `relay.ts`'s `request()` is the one caller and maps an abort to `RelayError` kind `timeout` ("The link relay did not answer in time." on both panes; `src/ppt/fetch.ts` fails a timed-out batch once instead of deferring every row). v2.7.13: `relay-reason.ts` (the relay's refusals in the pane's words: `TOO_LARGE` 413, `RELAY_BUSY` 429, `RELAY_FULL` 507, `RELAY_FAULT` other 5xx, `relayReason(error)`; a kind with no sentence answers undefined and the relay's own message travels on). Local links (v2.9, S1): `bundle.ts` (the clipboard bundle's JSON codec, `encodeBundle`/`readPastedBundle`, the `text/html` `data-plsfix-links` carrier plus the `text/plain` `BUNDLE_SENTENCE` fallback, `BUNDLE_MAX_CHARS` 25 MB), `local.ts` (`LOCAL_REV_BASE` 2**40, `nextLocalRev`, `previousRevOf`, the fixed all-zero `localWorkspace`, so a local rev never equals a relay one), `local-collector.ts` (`LocalCollector implements RelayApi`: Excel's write path, records one export or copy's links plus an inbox row per link into a bundle instead of the network), `local-store.ts` (`LocalStore implements RelayApi`: PowerPoint's read path, `ingest()` keeps the two highest revisions per link and evicts past `LOCAL_STORE_BUDGET` 32 MB oldest-pasted first, unknown links answer `notPasted`), `local-persist.ts` (the `LocalPersistence` port: IndexedDB database `plsfix-links`, stores `links` + `inbox`, write-through; `memoryPersistence()` when IndexedDB is refused), `transport-setting.ts` (`LinkTransport` `local`/`relay` under `plsfix.link.transport.v1`, `loadTransport` defaults to `relay` only when `plsfix.link.workspace.v1` already holds a key, else `local`).
- `src/ppt/`: `host.ts` (the PowerPoint Office.js code every link flow goes through: scan by tags, insert dispatched on the payload kind, refresh in place - one shape or a whole update-all batch in a single sync, pictures only - or by reinsertion, break, go to slide, read the active slide), `charts.ts` (a chart link as a tagged group of native shapes: `chartPlan` from the payload's `ChartData` through `layoutChart`, `declineReason` - PowerPointApi 1.8 for groups, 1.10 for pie wedges, the per-host shape budget `SHAPE_BUDGET_WEB` 30 / `SHAPE_BUDGET_DESKTOP` 200 - `insertChart`, `refreshChart` rebuilding at the same corner and width, a group whose chart stopped being drawable becoming a picture); `chartPlan` also fits a chart taller than the slide (`CONTENT_HEIGHT`) + `chart-draw.ts` (`drawGroup`: primitives to shapes in syncs of `SHAPES_PER_SYNC` 12; since v2.8.14 every label text box is written with `wordWrap` false and `autoSizeSetting` AutoSizeNone (PowerPointApi 1.4) so a label never stacks ("1 / 519") - a falling or flat line is `addLine(straight)`, a rising one the `GeometricShapeType.lineInverse` preset, the host's other diagonal - wedge adjustments in the sync after their add, then `addGroup` + name + both tags; a rejection after the first sync runs `chart-cleanup.ts` `cleanupShapes` - a fresh `PowerPoint.run` that deletes every id a prior sync already confirmed, then sweeps the slide top level for every shape still named for the chart (`chartPrefix`: the primitives and, on the Mac, the `: tier` sub-groups are named in the batch that adds them), because a refused batch keeps the adds queued before the call it refused (Mac 16.107, 14.09: four sub-groups with no id) - before rethrowing the original error unchanged), and every sync of the draw runs under `withSyncDeadline(work, what)` with `SYNC_TIMEOUT_MS` 60 000: a swallowed batch runs the same cleanup, rejects `ChartDrawTimeout`, and `charts.ts` inserts the picture with `CHART_HOST_SILENT` as the note (a swallowed refresh replaces the group with the picture at its corner and width and hands the note back); since v2.6.13 `host.ts`, `picture.ts`, `tables.ts`, `texts.ts`, `placement.ts`, `shapes.ts` and `object-tools.ts` run every sync under the same deadline with their own wording, and `paintBatch` fails a hung repaint batch once instead of row by row, `texts.ts` (a text link as one tagged text box: `addTextBox` on PowerPointApi 1.4, auto-sized to its text, placed in free space; a refresh writes the text and the tag only), `tables.ts` (native tables on PowerPointApi 1.8: `addTable` with `uniformCellProperties.font.size` = the payload's modal size (`uniformSize` in `table-font.ts`, so PowerPoint never sizes the rows for its 18 pt default and then keeps them tall) plus the per-cell text, font, fill and alignment writes in `table-cells.ts` (`writeCellsInChunks`, `writeCell`, a cell at the uniform size skips its size write), `CELLS_PER_SYNC` 8 cells per round trip because a formatted cell cost about 0.4 s per property on PowerPoint for the web on 09.09 and a whole 6x4 repaint in one batch ran past the deadline, the tag travels with the last chunk, an insert whose format chunk fails or times out takes its half-formatted table down again; a repaint rewrites the cells where the table sits and rebuilds it at the same corner when the grid changed); since 16.09 `table-style.ts` owns the header band on insert and rebuild (`styleNewTable`: `hasTableStyle` gates PowerPointApi 1.9, `readTableStyle` the one extra round trip, `queueDefaultStyle` fixing a table PowerPoint handed over with no style to Medium Style 2 Accent 1, `queueHeaderRow` setting `styleSettings.isFirstRowHighlighted` from the payload's `h` - a repaint sets it blind, no read first) and which cells a repaint may clear (`fillsToClear`, `readPaintTag` off the shape's own `PLSFIX_PAINT` tag: with one, only a cell `tables.ts` itself filled before and the source has since dropped; without one - a deck painted before this shipped - every cell but row 0 when the source has a header row); a hidden source column beside visible ones takes `HIDDEN_COLUMN_WIDTH` 48 pt, all hidden takes the content width in even columns, `placement.ts` (since v2.8.0 `resolveTarget` turns an `InsertTarget` - slide + where - into a box: `Free space` keeps the old `placeInFreeSpace` scan of the other shapes' boxes over a 960x540 slide, margin 36, gap 12 - empty layout placeholders and dashed empty frames excepted, a group whose own box is zero occupying the union of its children; a scan that still fails fits the object into the largest remaining rectangle instead of a full-size overlap; `Selected shape` fits inside the selection's box and removes an empty layout placeholder after the insert, any other shape left in place; the halves, quarters and `Whole slide` spots are rectangles of the content area - the slide minus a 36 pt margin - with the object's aspect kept and centred; `This slide` + `Free space` reproduces the pre-v2.8.0 behaviour exactly), `target.ts` (the Inbox tab's `#insert-slide` / `#insert-where` pickers - `This slide` or a named slide, `Free space` or a named spot - read into that `InsertTarget`, applied by every Insert button and by "Paste latest linked"), `picture.ts` (the selection-API picture insert, the only route below 1.8), `shapes.ts` (where a shape sits: the shape property list, the PowerPointApi check, walking into groups and addressing a shape through its `groupPath`), `links.ts` (orchestration: list, update, inbox, and the shared `applyBatch` repaint, cut into host batches by `batching.ts`); `updateLinks` asks the relay about every row, the cached status only draws the list; a 413 and a relay that cannot be reached arrive as sentences, `batching.ts` (pure: `planBatches` packs keys greedily under a byte budget, `REPAINT_BUDGET_BYTES` = 8 MiB of payload per `PowerPoint.run`, an item past the budget travels alone; `chunk` cuts a list to a route's item ceiling, used by both the status poll and the batch fetch), `revert.ts` ("Revert last update": repaint the ticked rows from the revision below their tag and write that rev back), `change-source.ts` (pure candidate ranking plus the re-point itself: `host.retagLink` then the shared repaint, both driven by a `FoundLink` carrying the new link's tag and token); `sameFamily` keeps text, table and picture links to exports of their own kind, `chooser.ts` (the "Change source" picker's three buttons and its enabled-with-one-tick rule), `main.ts` (pane boot, state and one guarded handler per button), `commands.ts` (`registerCommands(deps)`: the PowerPoint ribbon's `PLSFIX_PPT_*` FunctionNames - the Objects group's Object tools / Match size / Select similar / Swap / Capture style / Apply style and the Arrange group's six alignments and two distributions - each on its own promise chain into the pane's toast, `Object tools` = `Office.addin.showAsTaskpane()` then the Tools tab; `commandTable` is the one list `manifest/xml.test.ts` holds against `manifest/spec.ts`), `views.ts` (DOM renderers: the link table as folders - since v2.8.18 one `link-folder` header row per project, "Amasty · 3" counting the rows shown, then the rows, each Object cell carrying the workbook and kind on a `link-meta` second line, five columns, no `data-key` on a header - inbox list, candidate list, status and age labels), `actions.ts` (pure: row keys, which rows a button acts on, update detail lines, `filterRows` over the search box plus the status, source-workbook and slide filters with `linkSources` / `linkSlides` feeding the two selects - a ticked row outside the filter stays ticked), `links.ts` also `latestInboxItem` (the inbox sorted newest-first by `createdAt`, what "Paste latest linked" inserts in one click); `object-math.ts` (pure geometry of the Tools tab: `alignBoxes` against the selection bounds, `distributeBoxes` with the outer edges fixed, `matchSize` to the first selected box, `swapBoxes`, `sameKindAndSize` within 1 pt) + `object-tools.ts` (the Tools tab's Office.js on PowerPointApi 1.5: `getSelectedShapes` in selection order, one load sync and one write sync per action, `selectSimilar` through `getParentSlide` + `setSelectedShapes`, the Smart Painter's captured solid fill + outline kept in memory for the pane's lifetime); also `fetch.ts`, `POST /api/links/fetch`, `MAX_FETCH_ITEMS` 200 / `FETCH_BLOB_CAP` 4 MiB per response beside the 8 MiB repaint budget. Since v2.7.3 `charts.ts` refuses a plan the slide's height fit shrank below `MIN_SIZE` 200 x 120 pt (`CHART_TOO_SMALL`), bounds the placement itself (`minPlacementScale` through `placeOnSlide`'s `minScale`, so an occupied slide gives a full-size overlapping group rather than a shrunken one) and keeps a backstop picture at the placed box (`drawPlaced`, `pictureSynced` shared with the timeout fallback). Since v2.7.13 (P3 stress pass): `missing-shape.ts` (the one answer for a shape a row still names and the deck no longer holds where it held it: `isMissingShape` reads office.js's `ItemNotFound`, `missingShapeError` rewords it as "<stage>: that object is no longer where the list had it."; wired at `refreshLink` / `breakLink` and the catch in `texts.ts`); `refresh.ts` (every repaint of a link already in the deck: `tagFor`, `writeTags`, `supportsInPlaceRefresh`, the one-round-trip `refreshLinks` / `queueRefresh`, the reinsertion fallback below PowerPointApi 1.8; `host.ts` re-exports them, so callers still reach the whole adapter through `./host`, and owns only the scan, the insert, the retag and the break); `tables.ts` also `requireTableSize` (the 60 x 20 cap checked again on the way in, a payload past it costs a round trip per eight cells) and a rebuild that keeps the shown revision's tag until the last format chunk lands; `object-tools.ts` `selectSimilar` refuses a reference the slide's shape collection cannot see (a grouped child) instead of selecting nothing; `pane-details.ts` (`createPaneDetails`: the details buffer under the toast, read once and cleared by the guard's `finally`); `inbox-queue.ts` (`queueOrder` newest first minus the exports this pane pasted, `rememberPasted`, `latestInboxItem`; keeps a delete the relay refused from turning the next one-click paste into a duplicate); `commands.ts` takes the pane's guard as `CommandDeps.run`, so the ribbon shares the busy latch instead of queueing behind a batch; `main.ts` runs one action at a time through it. `transport.ts` (local links, S3): `PptTransport`, `openPptTransport`/`preBootTransport` decide which `RelayApi` (the real client or a `LocalStore`) and `Workspace` (the paired key or the fixed local one) every deck flow runs against, `setMode` opens the store lazily and persists the choice; `paste-links.ts`: `pasteLinks` (one deck scan, `LocalStore.ingest`, then `updateLinks` on the rows the bundle named; ids the deck does not hold yet only reach the Inbox) and `summarizePaste` ("Pasted 12 links: 9 updated, 3 up to date, 2 waiting in the Inbox.").
- `src/help/`: the help behind every section's "?", as data plus its coverage rule. `copy.ts` owns the two keys the feature turns on (a section's `data-help` else its `aria-labelledby`, a button's `data-action` else its id), `helpSections(root)` and the merged `HELP`; `copy.excel.ts` and `copy.ppt.ts` hold the sentences, split so each file stays under 400 lines. `copy.test.ts` parses the shipped `taskpane.html` and `pptpane.html` and fails both ways: a button with no sentence, and a sentence naming a button that is gone. The deck's Linked objects section carries `data-help="deck-links"` because the Excel tab already owns `links-heading`.
- `src/pane/`: the Excel pane's per-tab modules `src/main.ts` wires up. `shared.ts` (cross-tab state nothing may reach via `../main`: `actionButtons`, `toast`, `tabs`, the shared `guard`, `refreshSelection`, `renderActionState`, `APP_VERSION`, `isExcelReady`/`setExcelReady`, `DELETE_CONFIRM_MS`); the guard drains `lastUndoSkipped` in its `finally` backs `dispatch.ts` (the `dispatch(action)` switch every `[data-action]` button and the styles-delete confirm run through the guard); exact cases stand before the prefix branches, so a longer id is never shadowed and `commands.ts` (`registerCommands`, the ribbon/shortcut `PLSFIX_*` table, its own promise chain per command since these can fire with the pane closed); `trace-panel.ts` (audit overlay toggle + Smart Track precedents/dependents walk with a back stack), `paint-slots.ts` (the three captured-format slots, saved with the workbook since v2.5.0 and mirrored to localStorage as the first-run fallback; the workbook's slots replace the machine's once Excel is connected, unless a capture already landed while that unawaited boot read was in flight), `reconcile-panel.ts` ("Find a combination": target + tolerance inputs, the result line - cells, sum, variance - in the house number style), `brand-tab.ts` (palette/font/currency/language settings, logo color picker, JSON import/export) + `brand-io.ts` (the palette as a file: `readPaletteFile` refuses a file with none of the palette keys, `copyPaletteJson` owns the clipboard fallback), `workbook-tab.ts` (sheet explorer, broken-name scrubber, contents sheet), `share-panel.ts` (prepare for sharing), `model-check-panel.ts` (the model check's list, its jump per row and the Copy button), `find-panel.ts` (Super Find), `styles-panel.ts` (unused-style scrubber); `links-tab.ts` (the Excel "Links" tab: export, workbook link list, push, go to source, remove, link key; `installLinksTab` is the only thing `main.ts` adds for it; `watchForRefresh` re-reads the list at most once every 2 000 ms on a worksheet edit, via `link-list-watch.ts`, while the tab is the one showing) + `links-charts.ts` (the chart picker, redrawn on tab-open and on a sheet change) + `links-list.ts` (the link table renderer) (since v2.8.12 the row's second line is the kind and where it points, "Table · P&L!A10:H24", the internal `PLSFIX_LINK_` id rides in the row's `title`; a `<colgroup>` pins the tick column at 24 px and the status column at 116 px because `table-layout: fixed` read its widths off a colspan project header; the Project select sits on its own row above New project / Move to project) + `links-toggles.ts` (auto-push and highlight tick boxes); `tool-search.ts` (`toolEntries` catalogues every `[data-action]` button plus any id-keyed button the help copy covers, across all four tabs; `installToolSearch` wires the box - typing, arrow keys, Enter/click running a row through `tabs.activate` then a real `button.click()`, Escape, the "/" shortcut - installed once `main.ts`'s `boot()` has the final DOM). `workbook-tab.ts` also owns the six sheet-tool buttons above the explorer and the "include very hidden" tick box (each acts, re-renders the explorer, hands back its toast line). `commands.ts` `NEEDS_PANE` (`PLSFIX_FIND`, `PLSFIX_STYLES_SCAN`, `PLSFIX_SHARE`) names the shortcut actions whose whole answer is in the pane; they run behind `showPane()`, the one `Office.addin.showAsTaskpane()` helper `PLSFIX_SHOWPANE` also uses. `trace-panel.ts` also `startPrecedentsOfSelection` (the same panel, its list grouped by source cell under `.chip-group` labels, the "N precedents of M cells; K cells have no formula" line). `shortcuts-panel.ts` (v2.7.7: the Brand tab's "Keyboard shortcuts" section and the only `Office.actions` code in the add-in: the list fetched from the served `shortcuts.json` with a 5 s deadline, one full-width row per action, `applyShortcuts` = validate, `firstClash`, `areShortcutsInUse`, `replaceShortcuts`, re-read, and `resetShortcuts` = null for every id; a `filled` latch set only by a landed `getShortcuts` and dropped by a failed one, so Apply reads first when unset and refuses when the read fails, never sending a map built from empty boxes; the refill runs off a `MutationObserver` on the tabpanel's `hidden`, so keyboard and programmatic activation count, and a box typed into since the last read is never overwritten; `KeyboardShortcuts 1.1` re-checked on every open, a host without it shows one sentence with the boxes disabled; every `Office.actions` call under a 15 s deadline, its `code`/`debugInfo` copied onto the thrown error for Copy details); `dispatch.ts` `HOST_FREE_ACTIONS` (the shortcut card, `shortcuts-apply`, `shortcuts-reset`) skip the `isExcelReady()` guard because they are Office chrome, not workbook writes. `links-charts.ts` labels a chart already exported as a link (`PLSFIX_LINK_<id>`) as "Linked chart · <last 4>" (`chartPickLabel`; the option's value stays the real name; a UX call for Daniel). v2.7.11 (P2): `unhideAllSheets` names the very hidden sheets a pass left behind in both branches (`buriedNote`), so "1 sheet shown" never reads as "that was all of them" on a workbook the tick would still open up. Since v2.8.22 the busy latch disables every `.app-shell button` (not only `[data-action]`) and on release calls the `setRuleButtonsSync` hook `main.ts` registers, which hands Back, Copy report, the two deletes and both key buttons back to their own modules (`renderKey`); without a supported Excel the Links tab's 16 buttons refuse through the guard with `EXCEL_NOT_CONNECTED_MESSAGE` instead of sitting dead; the tool search offers only buttons that can act (`canAct`); a ribbon or shortcut error shows the pane before its toast (`commands.ts`, and `src/ppt/main.ts` for PowerPoint). `links-transport.ts` (local links, S2): the per-device `local`/`relay` choice (`src/link/transport-setting.ts`) and `copy()`, which starts the clipboard write before its own first await (WebKit needs it inside the press) then runs the Excel work against a `LocalCollector`; `retryCopy()` behind the Copy for PowerPoint bar; relabels Push selected/Push all to Copy selected/Copy all and hides the Link key section in local mode.
- `server/`: `lib.rs` router + cache middleware + `/version` (the `version` field the gateway parses, plus a `relay` object counted per request: links, revisions, inbox rows, bytes, `max_bytes`), `store.rs` sqlite (WAL, auth hash, 2 revs, TTL sweep 30 d links / 7 d inbox, `get_link_rev` for one named revision, `touch_links`, the byte and row counts) + `store_inbox.rs` (the `inbox_v2` rows and the count the cap reads), `relay.rs` `/api` routes (bearer, ETag, body limits, `GET /api/links/{id}?rev=<n>` for the revert path) + `relay_inbox.rs` + `relay_touch.rs` (`POST /api/links/touch`, at most 200 items, the boot-time TTL refresh) + `relay_gates.rs` (what a write passes first: 429 with `Retry-After`, 507 past `MODELIS_MAX_BYTES` 1 GiB or `INBOX_MAX_PER_WS` 500 rows, each after a sweep that may heal it) over `limits.rs` (a token bucket per client key with an injected clock, `MODELIS_RATE_WRITE_PER_MIN` 100 / `MODELIS_RATE_WRITE_KIB_PER_MIN` 65536 / `MODELIS_RATE_READ_PER_MIN` 1200; `TrustedProxy` from `MODELIS_TRUSTED_PROXY` decides which header may name a client - unset trusts nothing and the peer address is the key, `cloudflare` is `CF-Connecting-IP`, `xff` the LAST `X-Forwarded-For` hop - keys truncated at 64 characters, the map capped at `MODELIS_RATE_MAX_CLIENTS` 10 000 with oldest-idle eviction; the hosted unit sets `cloudflare`), `main.rs` env + bind (`MODELIS_BIND`, default 127.0.0.1; the container image sets 0.0.0.0) with ConnectInfo + hourly sweeper (dead rows and idle buckets); `manifest.rs` (`GET /manifest.xml`: the file `MODELIS_MANIFEST` names, default `manifest.prod.xml` in the working directory, served verbatim for the hosted add-in or re-pointed at `MODELIS_PUBLIC_URL` - every hosted URL, the AppDomain and the Id, which becomes UUID v5 of the URL - so a self-hosted copy never collides with the hosted add-in; `AppState.manifest` carries the source, the route sets no-cache); also `fetch.rs`, `POST /api/links/fetch`, `deferred`. Since v2.7.12 (S1 hardening): `relay_gates.rs` (what a request passes first: the per-client rate limit by request and, for a write, a second one by body size - `charge_bytes`, one token per KiB of `RATE_TOKEN_BYTES` 1024 - plus the `ClientKey` the middleware resolves once; 429 with `Retry-After`, and the log line a full store gets); `store_room.rs` (how full the store is: the blob-byte sum, the two ceilings a write is checked against inside its own lock, `Caps` on the `Store` as the one place the ceiling lives, and the single-flight `CountsCache` behind `/version` for `VERSION_COUNTS_TTL` 30 s, cleared by the sweeper); `blocking.rs` (`store_call`: every route's store call on `spawn_blocking`, the hourly sweep included, because sqlite blocks the thread it runs on and the 30 s deadline cannot preempt a blocked worker); `refused.rs` (the refusal enum and the one JSON body each answers with); the payload router's two write methods carry `GlobalConcurrencyLimitLayer` (`MODELIS_MAX_INFLIGHT_WRITES` 32, clamped to at least 1, one shared pool - a plain `ConcurrencyLimitLayer` gives each method its own) with `RequestBodyTimeoutLayer` (`DEFAULT_BODY_DEADLINE_SECS` 10, a constant because `writes/min x deadline / 60 < permits` must hold) inside it, a link GET outside both; `/healthz`, `/version`, `/manifest.xml` spend the read allowance; `/api/*` answers are `no-store, private` with `Vary: Authorization`; `main.rs` prints the trust mode at startup and warns when every client would share one bucket.
- `scripts/`: the gates and generators npm calls: `build-manifests.ts`, `build-functions-metadata.ts`, `build-shortcuts-page.ts` (`public/shortcuts.html` from `public/shortcuts.json`, the hand-kept shortcut list and keys; `--check` gated as `npm run shortcuts:check`), `check-version.ts` + `version-rules.ts`, `build-ribbon-icons.ts` (the ribbon PNGs, see Entry points), and `ux-check.mjs` -> `scripts/ux/` (the pane UX gate: `browser.mjs` finds playwright-core and a Chromium headless shell, `dev-server.mjs` reuses or boots vite, `panes.mjs` + `fixtures.mjs` seed each pane state through the real renderers, `page-probe.js` is the injected in-page measurement, `report.mjs` the wording). `npm run ux:check` walks both panes at 320/360/420/500 in five states and fails on horizontal scroll, a clipped tab strip or label, a short tap target, sub-11px text, a toast over the tabs or an invisible Tab stop. `click-sweep.mjs` (`npm run ux:sweep`) boots both panes with no Office host and clicks every control (see Run / test). `scripts/rig/` is the web verification rig (README inside): `drive.mjs` runs one Playwright snippet against the scratch Chrome on CDP 9222 (`npm run rig -- @snippet`), `serve-manifest.mjs` serves the manifests to Office on the web (`npm run rig:manifests`), `snippets/` holds the sideload, pane-open and chart-proof runs; manual only, never CI.
- `demo/`: Rust generator (`rust_xlsxwriter`) of the play-around workbook `demo/out/pls,fix Demo Model.xlsx` (git-ignored): a DemoCo model whose sheets feed every tool (P&L with two planted errors, bridge table + charts, tornado block, rounding split, wide grid, hidden sheet, a broken name), since v2.8.12 a `CAGR` sheet between Variance and Data (`sheets/cagr.rs`: six series rows, the ribbon CAGR target column H, `=PLSFIX.CAGR` in I and the hand formula in J, expected values in the note row) and a "Start here" checklist; `layout.rs` is the one row/column map, `pen.rs` counts writes and `tests/workbook.rs` reconciles the saved file against that count. `npm run demo` builds it, restores the prod manifest into both wef folders and opens Excel + PowerPoint. Since v2.8.0: `src/sheets/guide.rs` (`GUIDE_ROWS`) is the 7-row guide band every visible sheet opens with (a "Try on this sheet" task list plus a Commands line), which pushed the model down 7 rows (the P&L export range is now `P&L!B11:E16`); `deck/` is the python-pptx generator of the tracked demo deck `pls,fix Demo Deck.pptx` (`npm run demo:deck`), shipped beside the workbook on every release.
- `video/`: the 87,5 s motion-graphic video of the add-in (`plsfix-video`: the Planner video's Rust engine, the HTML composition `comp/`, the Playwright capture `capture/` over the web rig because the pane lives in cross-origin frames). `cd video && cargo run --release -- all` = capture in Excel and PowerPoint for the web (the scratch rig profile; the demo workbook + deck uploaded to a fresh `plsfix-video/run-*` OneDrive folder; a new link key per run), render, audit (80-90 s, provenance = the demo build's sha256 + the pane version), deliver to `~/Desktop/pls,fix video.mp4` only when the copy there matches `video/delivered.sha256`. Spec `docs/superpowers/specs/2026-09-26-plsfix-video-design.md`, `video/README.md`.
- `test/`: `fakehost.ts` (in-memory Excel host, strict load semantics; `helpers.failNextImage` / `failNextChartRead` / `failNextCellProperties` / `failNextSync` arm one refusal each, queued so a retry can meet a second one, and a refused batch still applies its writes like real Office.js; `worksheet.getRanges` for the reconcile select); `cellCount` saturates to -1 past 2^31-1 like Excel (`rawCellCount` inside), a protected sheet refuses row-height and column-width writes, `context.application` is policed, `helpers.fireStructuralChange` fires a rows-deleted event, `fakeppt/` (PowerPoint fake: `model.ts` the deck, `objects.ts` the office.js objects over it - slides, shape collections and their adds, shapes, groups, tags, `format.ts` the fill, line, adjustments and text frame hanging off a shape, `tables.ts` `addTable`/`getTable` and the cell writes, `selection.ts` the PowerPointApi 1.5 selection surface - `getSelectedShapes` in selection order, `setSelectedShapes` same slide only, `getParentSlide` - with `helpers.selectShapes(ids)`, `size-guard.ts` (a negative width or height throws `InvalidArgument` on an add and on the setters, as the host does), `helpers.hangNextSync(afterSyncs?)` a sync that never settles, its own adds rolled back (lessons 08.09), fill `transparency` and line `weight`/`dashStyle`/`style` answering null once cleared or invisible like the host, `strict.ts` the load rules; `shapes.test.ts` covers the chart primitives - text boxes, lines, `addGroup(ids)`, a pie's adjustments after its first sync) + `fakerelay.ts` + `fakepng.ts`, `clock.ts` (virtual `Clock` for the debounced auto-push window), `hung-sync.ts` (`settleHungSync`: the one fake-clock settle loop the "host never answers" suites share, a real event-loop turn per step, lessons 12.09), `chart-placement`/`multi-area`/`merged`/`protection`/`tornado`/`universal` `.integration.test.ts` (the universality suites: where a chart lands, every area of a ctrl-clicked selection, merged and empty cells, a protected sheet, and each tool on the selection a modeller actually makes), `ppt.support.ts` (shared `bootPpt`/`seedLink`/`pushAgain`/`memoryStore` for the `ppt.*.integration.test.ts` suites, split by feature so each file stays under 400 lines; `ppt.perf.integration.test.ts` holds the round-trip budgets, counted by `helpers.syncCount()`), `links.chart` (a chart export carries its data) and `ppt.charts` (insert as a group, rebuild on refresh, the budget and API fallbacks), `*.integration.test.ts`. `fakehost.ts` also knows the `BarStacked` chart type, `series.format.fill.clear()` / `format.line.lineStyle` and `ChartAxis.numberFormat` (v2.7). `fakehost.ts` also `Range.delete` (one pass over the populated cells; merges the band covers are dropped, merges past it shift up), `charts.getCount` / `shapes.getCount`, `workbook.protection` with `helpers.protectWorkbook()`, `helpers.setIndent` (v2.7). `fakehost.ts` also answers `Range.rowHidden` (true / false / null for a mixed band) over `FakeSheet.hiddenRows` with `helpers.hideRows` (v2.7.8), and since v2.8.20 `worksheet.autoFilter` (`enabled`, `isDataFiltered`, `getRange()` a null object with no filter, strict loads) over `FakeSheet.autoFilterRange` with `helpers.applyFilter(range, hiddenRows)`. `fakehost.ts` also `FakeSheet.names` (sheet-scoped names), `NamedItemProxy.delete()` finding a record's real list, `FakeSeries.showLeaderLines`, `helpers.failNextSync(error, afterSyncs)` (v2.7.9), `helpers.refuseNextSync(error, afterSyncs)` (v2.8.9: the adds of that batch stay on the deck and its loads are lost, the way a real refusal leaves a slide). `stress.comps.support.ts` + the three `stress.comps.*.integration.test.ts` suites (v2.7.10: the adversarial pass over the comps tools - every selection, cap boundary, odd value, host level and repeat press - through the real `src/pane/dispatch.ts` with only `src/pane/shared.ts` mocked; `expectSentence` holds every answer to a line opening with the tool's own name; the 10 `it.skip` rows that pinned the refused-write Undo slot, the raw host string through `describeError`, the double-press second chart and the hidden/filtered rows all run green since v2.8.20). `stress.hygiene.*.integration.test.ts` (six files, v2.7.11: the P2 stress pass, every hygiene tool and format cycle pressed with the wrong thing selected, at the wrong time, twice; `stress.hygiene.pane` drives the shipped `taskpane.html` in jsdom through `src/pane/shared.ts`'s guard and `dispatch(action)` and asserts the toast, `stress.hygiene.sheets` the real `workbook-tab` on the shipped HTML, the other four the adapters; the 3 `it.skip` rows that pinned the partly-unlocked-sheet sentence, the refused-write Undo slot and the move receipt counting hidden tabs run green since v2.8.20). `stress.ppt.support.ts` + `stress.ppt.{tools,tables,links,payload,pane,relay}.integration.test.ts` (v2.7.13: the P3 stress pass, the shipped `pptpane.html` over the fake host and relay through `src/ppt/main.ts`, every tool with the wrong selection, at the wrong time, twice, against a host and a relay that answer badly; no skips); `test/fakeppt/office.ts` also `Office.actions.associate` with `helpers.commandIds()` / `helpers.runCommand(id)`, so a pane test can press a ribbon button.

Data flow, link export (v2): selection -> `src/excel/links.ts` (hidden name anchor + registry in `workbook.settings`) -> `Range.getImage`, or `src/excel/link-table.ts` for a table link (also `headerRow`, since 16.09: row 0 is a header when every one of its non-empty cells came out bold, which the rich read path turns into the payload's `h`; the plain fallback never sets it), or the range's text for a text link -> `src/link/model` encodePayload -> `src/link/crypto` seal -> `src/link/relay` putLink -> `server/relay.rs` -> `store.rs` (ciphertext only) -> inbox item sealed with the workspace key. A chart source also ships `ChartData` (`src/excel/link-chart.ts`) beside its PNG. PowerPoint: `src/ppt/links.ts` listInbox -> open -> `src/ppt/host.ts` insertLink (rectangle + picture fill + tags, or `tables.ts` addTable + cell formats + tags, or `texts.ts` addTextBox + tags, or `charts.ts` drawing the chart's primitives as a tagged group where the host and the budget allow), placed in the slide's free space (`placement.ts`); the pane toasts when the object had to overlap. Update: scanLinks (tags, groups walked three levels deep on PowerPointApi 1.8) -> relay status -> `src/ppt/fetch.ts` fetchLinks (one batch for the whole deck; getLink per row only for what came back deferred); `updateLinks` no longer trusts the cached status, every row is asked; a relay that cannot be reached fails every row once -> open -> `refreshLinks` (`fill.setImage` on the shape each `groupPath` leads to, geometry untouched, one `PowerPoint.run` and one sync per 8 MiB of payload (`batching.ts`); a host below 1.8, one that refuses a shape, or a batch holding a table falls back to `refreshLink` per row, which repaints a table cell by cell). Revert: `src/ppt/revert.ts` getLinkRev(tag.rev - 1) -> open -> the same repaint with the older rev in the tag, so the newer one reads as available again. Change source: `src/ppt/change-source.ts` getLink(new token) -> open -> `retagLink` (both tags, geometry untouched) -> the same repaint -> deleteInbox; the old link stays on the relay and nothing in Excel moves. Data flow, local links (S1-S3): Excel copy -> `src/pane/links-transport.ts` copy() -> `LocalCollector` (the same payload `putLink`/`postInbox` would have sent, revs above `LOCAL_REV_BASE`) -> `src/link/bundle.ts` encodeBundle -> `src/ui/clipboard-links.ts` beginClipboardWrite (or `copyBundleNow` behind Copy for PowerPoint) -> the OS clipboard -> PowerPoint's Inbox paste box -> `bundleFromPaste` -> `src/link/local-store.ts` LocalStore.ingest (persisted through `local-persist.ts` to IndexedDB `plsfix-links`) -> `src/ppt/paste-links.ts` pasteLinks -> `updateLinks` on the deck's own rows, an unseen id waits in the Inbox for Insert / Paste latest linked.

Invariants: link identity is the shape tags (`PLSFIX_LINK`, `PLSFIX_KEY`) in PowerPoint and the hidden name `PLSFIX_LINK_<id8>` in Excel, never an address or a position; the server holds ciphertext and `sha256(authKey)` only, and serves no dotfile (encoded forms folded first) on the Access-bypassed path; refresh never writes geometry except height on an aspect change, and a table only when its grid changed and it is rebuilt at the same corner; every Office.js scalar read is preceded by `load()` + `sync()` (the strict fakes throw otherwise); every Excel flow that writes ends on `syncWrite`/`paintSync` from `src/excel/protection.ts`, never a bare `context.sync()`, so a protected sheet or a merged cell answers the staged sentence; every cap is counted before a grid is loaded and a negative `Range.cellCount` (Excel answers -1 past 2^31-1) is over the cap; every PowerPoint `context.sync()` runs under `withSyncDeadline` (`src/ppt/chart-draw.ts`, 60 s, its own wording per site), because a web batch can be swallowed without applying or rejecting; the Excel and PowerPoint commands register at module top level (the shared runtime may fire one before the pane connects); manifests are generated, never hand-edited; `package.json` is the only typed version; hand-curated files (`README.md`, `ROADMAP.md`) get surgical edits only. A storage ceiling is read inside the same lock as the insert it guards (`store_room::room_locked` from `store::put_link` / `store_inbox::post_inbox`), never before the write; no handler touches `state.store` directly, every store call goes through `blocking::store_call`.

Run / test: `npm run demo` (demo workbook + prod manifest sideload + both apps), `npm start` (Excel sideload with the dev manifest), `npm run start:ppt`, `npm stop` (restores the prod manifest into wef), `npm run check` (tsc, eslint, prettier, vitest, cargo test, manifest:check, validate, functions:check, version:check), `npm run ux:check` (both panes at 320/360/420/500, screenshots under `.superpowers/ux`), `npm run ux:sweep` (`scripts/ux/click-sweep.mjs`: both panes in the headless browser with no Office host at all, every button, tab, help toggle, select and checkbox clicked; fails on a dead control, a tab or help card that is not the only one open, a duplicate data-action or a Tab walk that never comes round; it proves every control reacts, not that it routes right: routing is `src/pane/dispatch.test.ts`, which the sweep cannot replace; a fresh worktree runs `npm run functions:js` first, or the sweep reports a taskpane 404 on `functions.js`), `npx vitest run test/clickthrough.excel.integration.test.ts test/clickthrough.ppt.integration.test.ts` (the click-through harness, slice H: every button of both panes - static and generated (sheet rows, a link row's tick, model-check findings, trace chips, find rows, an inbox Insert) - pressed over a CONNECTED fake host in several seeded states (fresh, a model block, adverse: multi-area/protected-sheet for Excel, unpaired/paired/objects-selected/a real link for PowerPoint) plus every ribbon command; complements `ux:sweep`, which proves a control reacts with no host at all, by proving it also answers right once one is there; a KNOWN_DEFECTS map at the top of each file names anything found that slice could not fix, and the suite fails once a fix makes the entry stop reproducing), `npx vitest run --coverage` (v8 census; the `*.audit.*` suites are the 09.09 audit wave, one per slice, plus `*.review.audit.*` for what the reviews added), `MODELIS_DATA=./data cargo run --manifest-path server/Cargo.toml`, deploy only via the gateway (`db deploy modelis`); release = `sh scripts/release.sh patch` after the reviewed merge, and the pushed tag runs `.github/workflows/release.yml` (build, demo workbook and deck, container image with a smoke test of `/healthz`, `/version`, `/taskpane.html` and a re-pointed `/manifest.xml`, GHCR push, GitHub Release with `manifest.prod.xml`, `plsfix-pane-<tag>.zip`, `pls,fix Demo Model.xlsx`, `pls,fix Demo Deck.pptx`); self-host proof = that workflow, no Docker runs on the Mac.

Where bugs live (symptom -> file): wrong cell/format written -> `src/excel/<feature>.ts`; a chart landed on the data -> `src/excel/chart-place.ts`; a ctrl-clicked selection refused -> `src/excel/areas.ts`; a typed number in a formula row not tinted, or the formula beside it striped -> `isTypedNumber` / `nearestFormulaAcross` in `src/audit.ts`; Fill formula right or down leaving raw decimals -> the `numberFormat` grid beside `copyFrom` in `fastFillAuto` (`src/excel/formulas.ts`); Precedents or Dependents from the ribbon selecting one cell of several -> `selectAreas` in `src/excel/trace.ts` (every area on the first area's sheet, one `getRanges`); a growth formula coloured as a partial input -> `IDENTITY_CONSTANTS` in `src/classify.ts`; a text label turned blue by Autocolor -> the `text` class in `src/classify.ts` and `classFont` in `src/excel/autocolor.ts`; the Links list showing an internal id or a fat tick column -> `whereItPoints` in `src/pane/links-list.ts` and the `<colgroup>` in `taskpane.html`; `PropertyNotLoaded` -> a missing `load()` in that adapter (reproduce in the fake host first); dead pane button -> `src/pane/dispatch.ts` or the `[hidden]` cascade in `src/styles/tokens.css`; a section with no "?", or a button missing from its card -> `src/help/copy.ts` keys, reproduced by `src/help/copy.test.ts`; pane scrolls sideways, a tab or a label clipped at a narrow dock -> the `src/styles/` sheet that owns that area, reproduced by `npm run ux:check`; chart labels showing more than the value -> `styleChartLabels` in `src/excel/internal.ts` and the position map in `src/chart-labels.ts`; a chart link arriving as a picture where the host could draw it -> the note beside "Inserted" or under "Update all" names the reason: `declineReason` in `src/ppt/charts.ts` for this host, `readChartData` in `src/excel/link-chart.ts` (`chartIssue`, the same sentence in the Excel toast) for Excel (budget, 1.8/1.10) then `src/excel/link-chart.ts` (no chart data: the kind, a blank value, the caps); a slide chart drawn wrong -> `src/chart-shapes.ts` (a line chart: `src/chart-shapes-line.ts`, then the rising/falling split in `src/ppt/chart-draw.ts`); Smart Painter refused by the host -> `paintShape` in `src/ppt/object-tools.ts` (null line and fill values are never written back); link not found in PowerPoint -> tags, `src/ppt/host.ts` scanLinks; "source missing" -> anchor resolution in `src/excel/links.ts`; relay 403/404 -> key derivation `src/link/crypto.ts` or TTL in `server/src/store.rs`; relay 429 or 507 -> the write gates in `server/src/relay_gates.rs` (per-client rate limit, byte ceiling, inbox row cap) and the env limits in `server/src/main.rs`; wrong picture size -> `src/link/status.ts`; a table inserted on top of something, or in the wrong place, a second Free-space insert landing on a Mac chart, or a dashed guide blocking the halves -> `src/ppt/placement.ts` occupiedBoxes then `src/layout.ts` reportedBox / placeInFreeSpace; a Free-space insert landing as a sliver, or overlapping full size with a note naming the largest free spot -> `fitHole` / `minScale` in `src/free-space.ts`; a chart placed over a text placeholder's empty lower half -> `textOccupiedHeight` in `src/text-extent.ts` and `loadTextExtents` in `src/ppt/placement.ts`; chart labels wrapped or stacked, or a dense chart's labels missing -> `addLabel` in `src/ppt/chart-draw.ts` then the slot rules in `src/chart-shapes.ts`; a deck that painted last week's figures after an Update all -> `assertFresh` in `src/link/status.ts` (called from `src/ppt/fetch.ts`); a link in the wrong project, or a new export joining none -> `src/link/project.ts` then Excel `src/pane/links-tab.ts` / PowerPoint `src/ppt/actions.ts` filterRows; the deck list's folders out of order, or a row without its workbook line -> `groupByProject` in `src/link/project.ts`, then `renderLinkRows` / `labelCell` in `src/ppt/views.ts`; a pane test or a rig counting a folder header as a link row -> select `#link-rows tr[data-key]`; an insert landed on the wrong slide or spot -> `src/ppt/target.ts` (the pickers) then `src/ppt/placement.ts` resolveTarget; wrong cell format in a deck's table -> `src/excel/link-table.ts` (what was read) then `src/ppt/tables.ts` (what was written); a deck's table inserted with double-height rows -> `uniformSize` in `src/ppt/table-font.ts` and the `uniformCellProperties` in `addTable`; "12,400" in a deck where Excel shows "12 400" -> `localizeNumberText` in `src/numbers.ts` and the separators batch in `src/excel/link-table.ts`; an overlay's pink tint shipped as a table cell's fill -> `originalFillColor` in `src/excel/fill-store.ts`; a deck's table shows no header band on the Mac -> `queueTableStyle` in `src/ppt/table-style.ts` (an API-created table has NO style and refuses every `styleSettings` load until a style is written blind: since v2.8.16 insert, rebuild and repaint write Medium Style 2 Accent 1, the header flag and an unbanded body, never reading); a table lost its header band or shows a stale fill -> `fillsToClear` in `src/ppt/table-style.ts`, then the paint tag; the header flag -> `headerRow` in `src/excel/link-table.ts`; a text link's box moved, resized or restyled on update -> `src/ppt/texts.ts` (a refresh may write the text and the tag only); a template block writes the wrong numbers or lands on top of something -> the grid in `src/template-blocks.ts` first, then the write in `src/excel/templates.ts`; a model check misses or misreports a cell -> `src/model-check.ts` rules first, then the reads in `src/excel/model-check.ts`; add-in not loading -> `manifest/spec.ts` + `npm run validate`; pane stuck on "Connecting" -> `src/host-ready.ts` (Office.onReady never settles when the custom-functions runtime fails to start, seen on Excel for the web; the host probe boots the pane degraded); Excel crash on `npm start` -> `package.json` `config.dev_server_port` + vite IPv4 (lessons); the shortcut card is stale or missing -> `scripts/build-shortcuts-page.ts` and `npm run shortcuts:check`; the search finds the wrong tool or none -> `src/tool-search.ts` ranking, then the catalogue in `src/pane/tool-search.ts`; Find a combination picks the wrong cells or none -> the solver, its tolerance and tie rule in `src/reconcile.ts`, then the numeric-cell read and the `getRanges` select in `src/excel/reconcile.ts`; an object tool moved or sized the wrong shape -> `src/ppt/object-math.ts` first, then the selection reads in `src/ppt/object-tools.ts`; "Object tools need PowerPoint 2021" -> the PowerPointApi 1.5 guard there (`getSelectedShapes`, `setSelectedShapes`, `getParentSlide` are all 1.5); the deck's link list hides rows -> `filterRows` in `src/ppt/actions.ts` and the select options `src/ppt/main.ts` rebuilds on every render; "Paste latest linked" inserts the wrong export -> `latestInboxItem` ordering in `src/ppt/links.ts` (relay rows are sorted by `createdAt` in the pane); a ribbon button shows no icon -> the button's `icon` in `manifest/spec.ts` and `public/assets/ribbon/<name>-{16,32,80}.png`; a PowerPoint ribbon button does nothing -> its FunctionName in `manifest/spec.ts` against the table in `src/ppt/commands.ts` (both hosts share one long-lived runtime, so the pane page must have loaded); the UX gate cannot find playwright-core -> `scripts/ux/browser.mjs` probes every `~/.npm/_npx/*/node_modules`, or set `UX_CHECK_PLAYWRIGHT_DIR`. Added 09.09 (audit wave v2.6.3 to v2.6.14): the pane shows Excel's own "The worksheet X is protected." instead of a pls,fix sentence -> that flow's last sync must be `syncWrite`/`paintSync` from `src/excel/protection.ts`; a whole-sheet Ctrl+A freezes the pane, or a scan reads a sheet whole -> `overCap`/`pickScannableSheets` in `src/excel/internal.ts` (`cellCount` -1); a fill or paste wrote outside the selection -> the extent maths in `src/excel/formulas.ts`; a formula coloured as an external link -> `hasWorkbookReference` in `src/classify.ts` (a bracket is another workbook only when a sheet name and "!" follow it); the audit overlay reads On with no stripes, or the highlight refuses for an overlay nobody can see -> the empty-keys check in `src/excel/audit.ts` and `painted` in `src/excel/fill-store.ts`; a chart restyle fails on Excel for the web -> the tolerated surface batch at the end of `formatSelectedChart` in `src/excel/charts.ts`; a chart landed far below the data -> the null size fallback in `src/excel/chart-place.ts`; Find a combination refuses on an old build -> the ExcelApi 1.9 guard in `src/excel/reconcile.ts`; the separators note tells a correctly set Excel to change -> `separatorsMatch` in `src/numbers.ts`; an imported palette wiped the colours, or Copy palette JSON said nothing -> `src/pane/brand-io.ts` and `parsePalette` in `src/settings.ts`; the model check reports no unused styles -> `unusedStyleFindings` in `src/excel/model-check.ts`; the Links tab boots with its tick boxes clear and a list that never refreshes -> the boot order in `src/pane/links-tab.ts` (`touchLinks` runs last, `fetch` has no timeout); "turn the linked-cell highlight off first" with no tint -> `paint`/`undoPaint` in `src/excel/link-highlight.ts`; a push fails per link with a missing office.js method -> `requireImageApi` in `src/excel/link-render.ts`; an insert that worked reported as failed, or a duplicated shape after a retry -> `deleteInbox` in `src/link/relay.ts` (404 is done); a deck re-downloads a picture it already holds -> the weak ETag compare in `server/src/relay.rs`; anything but the built pane served under `/modelis/` -> `no_dotfiles` in `server/src/lib.rs`; Update all says up to date right after an Excel push -> `updateLinks` in `src/ppt/links.ts`; a re-pointed link shows the old table or answers "is no table" -> `sameFamily` in `src/ppt/change-source.ts`; a deck's table arrives with no width -> `tableSize` in `src/ppt/tables.ts`; an Insert button is clickable while an insert runs -> `renderInboxView` in `src/ppt/main.ts`; the pane stays busy for ever after any PowerPoint action, or loose untagged shapes stay on a slide -> the deadline in `src/ppt/chart-draw.ts` (every sync in `src/ppt/` runs under it), then the fallback in `src/ppt/charts.ts`; untagged `: tier` groups or stray chart shapes left on a slide after a refused insert -> `sweepByName` in `src/ppt/chart-cleanup.ts` (the slide top level, by `chartPrefix`), then which sync refused (`helpers.refuseNextSync` reproduces a batch the host applied and then refused); InvalidArgument "size must not be negative" on a chart insert -> the bands in `src/chart-shapes.ts`, then `bridgeRange` in `src/chart-shapes-parts.ts`; a chart group hanging off the slide -> `onSlide` in `src/ppt/charts.ts`; Smart Painter paints a blank line colour -> the guard beside `style.line.color` in `paintShape`, `src/ppt/object-tools.ts`; a pane button runs the wrong action -> the prefix branches in `src/pane/dispatch.ts`; a ribbon command or shortcut does nothing right after a cold launch -> `registerCommands()` must sit at module top level in `src/main.ts` and `src/ppt/main.ts`; "(too large for undo)" on the wrong message -> the guard's `finally` in `src/pane/shared.ts`; pls,fix Undo restores the cells and still toasts "The argument is invalid or missing or has an incorrect format." -> `settableProperties` in `src/excel/undo.ts` (a captured field Excel refuses on write; the fake refuses the same shape); a control of either pane is dead in a browser with no host -> `npm run ux:sweep`; a table update or insert says "stopped answering" on the web -> `CELLS_PER_SYNC` in `src/ppt/tables.ts` (fewer cells per round trip), and remember that an abandoned batch keeps running in the host and every later batch queues behind it. Added 12.09: a self-hosted `/manifest.xml` still pointing at dbautomatizacijas -> `MODELIS_PUBLIC_URL` unset (or equal to the hosted URL), then `rewrite` in `server/src/manifest.rs`; the release workflow red -> its smoke test (`/version` must carry the tag's version, the manifest no hosted URL) or `gh release create` on a tag that already has a release. Added v2.7 (comps and valuation tools): the comps block writes the wrong span, misses a numeric column or reads the header row as data -> `readCompsBlock` / `statsGrid` in `src/stats.ts`, then the write in `src/excel/stats.ts`; "Comps stats need six empty rows under the block." over rows that look empty -> `requireEmptyBlock` in `src/excel/internal.ts` on the target `src/excel/stats.ts` computed; a football field drawn upside down, or its floor series visible -> `styleFootball` in `src/excel/football.ts` (`reversePlotOrder` is ExcelApi 1.7, the floor is `fill.clear()` plus `line.lineStyle = none`); a football field whose bars all start at zero -> the helper block in `src/excel/football.ts` (the second column is `high - low`), then `footballField` in `src/chartmath.ts`; pinstripes band the wrong lines, or a second press bands instead of clearing -> `bandLines` / `alreadyBanded` in `src/excel/pinstripes.ts` (one hand-painted cell in a banded line makes the press a fresh band); pinstripes skipping or counting a hidden row wrongly -> `filterHiddenRows` there (a filter over the selection = band the visible rows, a hand-hidden row outside one counts); "Pinstripes: turn the audit overlay off first" with no stripes in sight -> `painted` in `src/excel/fill-store.ts`, the same check the two overlays make of each other. Added v2.7 (hygiene tools): a cycle button leaves the indent, alignment or underline where it was, or steps twice -> the canonicalisers in `src/cycles.ts` (Excel rewrote what it gave back), then `applyHygieneCycle` in `src/excel/format-cycles.ts`; Unhide all, Show only this, Bury this or a move does nothing, or reports Excel's own string -> `structureProtected` / `structureNote` in `src/excel/protection.ts`, then the tool in `src/excel/workbook.ts`; "Excel needs one visible sheet." on a sheet that looks buriable -> the visible-sheet count in `burySheet` (a very hidden sheet is not visible); Clean past the data removes nothing, or removes rows a chart sat on -> the two used ranges and `Drawings` in `src/excel/clean.ts`; `=PLSFIX.CAGR` shows `#VALUE!` on numbers that look fine -> `requirePositive` in `src/functions/index.ts`, then the `periods < 1` guard in `src/chartmath.ts`; a custom function has no help link in Excel's function list -> `HELP_URL` in `src/functions/metadata.ts` and the regenerated `public/functions.json`. Added v2.7.3 (PowerPoint and shell follow-ups): a chart link arrived as a picture saying it would be smaller than 200 x 120 pt -> `onSlide`, `belowMinimum`, `minPlacementScale` in `src/ppt/charts.ts` (the export is far taller than it is wide, or the slide is full); a bar chart's value label sits over the end of its bar, or is unreadable there -> `barLabel` in `src/chart-shapes-parts.ts` (the label would have crossed the box; dark fill = white ink); a stacked bar chart's segments in separate rows -> `barBars` in `src/chart-shapes.ts`; a chart link inserted as a picture saying "chart data unreadable" -> `guardChart` in `src/link/chart-guard.ts` (what Excel put on the relay: `readChartData` in `src/excel/link-chart.ts`); Tab walks through every tab of the strip, or the Workbook tab opens with stale sheets after an arrow key -> `src/ui/tabs.ts`; a keyboard shortcut ran and nothing appeared with the pane shut -> `NEEDS_PANE` in `src/pane/commands.ts`. Added v2.7.5 (relay timeouts, store-valid manifest): a relay call hangs with no toast for longer than 20 s -> `src/link/relay-timeout.ts`, then `request()`'s catch in `src/link/relay.ts`; "Paste latest linked" grabs the older of two same-second exports -> the `ORDER BY` in `server/src/store_inbox.rs`, then the stable sort in `src/ppt/links.ts`; a request holds a server worker open -> `REQUEST_TIMEOUT_SECS` / `app_with_timeout` in `server/src/lib.rs` (a blocking sqlite call inside a handler is not preempted by it: the relay-hardening slice moves the store onto `spawn_blocking`); the store validator flags the icon or the support URL -> `manifest/xml.ts` (`HighResolutionIconUrl`), `manifest/spec.ts` (`supportUrl`), and whether `icon-64.png` / `support.html` are deployed yet (`-p` validation answers "unreachable" for a file the server does not serve); `/support.html` or `/privacy.html` 404 -> the file under `public/` (no route: Vite copies it verbatim). Added v2.7.6 (audit tools): "Select consistent region" grabs too much or too little -> the growth order in `consistentRegion` (`src/audit.ts`), then the current-region read in `src/excel/consistent.ts` (a blank line is the ceiling by design); "Select consistent region supports up to 5,000 cells at once." -> the block around the active cell is over the cap (`src/excel/consistent.ts`; the sentence is `scanCapSentence` in `src/excel/audit.ts`); "Precedents of selection" misses a cell, or the pane stutters on a long batch -> `resolveGroups` in `src/excel/trace-many.ts`, then the grouping in `src/pane/trace-panel.ts`; a precedent on another sheet not selected -> by design, listed as a chip; a grouped precedents label beside its chips instead of above -> `.chip-group` in `src/styles/controls.css`. Added v2.7.7 (shortcut manager): "Custom shortcuts need Microsoft 365 with a signed-in account." on a host that should support it -> `actions()` in `src/pane/shortcuts-panel.ts` (the `isSetSupported("KeyboardShortcuts", "1.1")` check), then whether the user is signed in to Office at all; Apply refuses a key Excel accepts, or accepts one Excel refuses -> `parseCombo` in `src/shortcuts-model.ts`, then Microsoft's "Guidelines for custom key combinations"; Apply names a clash with an action the user never touched -> `firstClash` (effective keys: a shipped default counts); a remapped key does not fire -> `diffShortcuts` (null = revert), then Office's own conflict dialog and "Reset Office Add-ins shortcut preferences" in Excel's Tell me box; "Your current shortcuts could not be read, so nothing was changed." -> `fill` and the `filled` latch in `src/pane/shortcuts-panel.ts` (a read that fails after a success drops it); the section empty with "The shortcut list could not be read." -> `fetchShortcuts` (relative `shortcuts.json`, 5 s). Added v2.7.8 (paste suite): a duplicated formula moved a reference it should not have, or left one it should have moved -> `insideBlock` / `sameSheet` in `src/formula-duplicate.ts`, then the token boundaries in `src/formula-refs.ts` (reproduce in `src/formula-duplicate.test.ts` first); a duplicated formula on another sheet reads the wrong sheet -> the `toSheet` prefixing in `src/formula-duplicate.ts` (a 3-D span is judged by its last sheet, by design); "Paste target overlaps the copied block." on a target that does not -> `blockRect` + `overlaps` in `src/excel/paste.ts`; a number-format paste filled the wrong cells or too many -> `tileGrid` in `src/paste.ts`, then `planFormatWrites` in `src/excel/paste.ts` (grow only when smaller in both axes); a row-height paste set every target row to one height -> the per-row loop in `pasteRowHeights`; a row-height paste skipped a row that is not hidden -> `Range.rowHidden` is null for a mixed band, the loop reads one row at a time. Added v2.7.9 (Excel follow-ups): a fast fill runs unbounded off a large selection -> the extent check in `fastFillAuto` (`src/excel/formulas.ts`); Super Find or prepare-for-sharing refuses a real deal model's sheet -> `SHEET_SCAN_CELL_CAP` in `src/excel/internal.ts` (the result line names the skipped sheets and the cap); a broken name scoped to one sheet never lists, deletes or reaches the model check -> `loadSheetNames` / `brokenEverywhere` in `src/excel/internal.ts`; "The workbook's structure is protected, so nothing was deleted." -> the refused sync in `deleteBrokenNames` (`src/excel/workbook.ts`; a real host may have deleted the earlier items of the batch: launch check); a pie restyled below ExcelApi 1.19 loses its leader lines -> `applySeriesLeaderLines` in `src/excel/charts.ts`; the chart picker shows a `PLSFIX_LINK_` name -> `chartPickLabel` in `src/pane/links-charts.ts`; "Too large to read" in the model check panel with no count -> `src/pane/model-check-panel.ts` (still the old wording, a follow-up). A tornado or football refusal that ate an Undo slot, or a helper block written over something -> `writeHelperBlock` in `src/excel/chart-blocks.ts` (empty check, then protection, then the capture); a comps table's label or text column reset to General under the statistics block -> `statsFormats` / `writeStatsFormats` in `src/excel/stats.ts`; "Pinstripes: turn ... off first" naming the wrong overlay -> `requireNoOverlayOwner` in `src/excel/fill-store.ts` and each `FillStore`'s `label`; a comps tool answering office.js's own string with no tool name in front ("The sync failed.") -> the error lost its `code` on the way (since v2.8.20 `describeError` in `src/ui/report.ts` prefixes only an error carrying one), or the guard did not pass the action id. A size-cycle refusal that opens mid-sentence or does not name the stage -> the two strings in `src/excel/sizes.ts`; "1 sheet shown" on a workbook still hiding sheets -> `buriedNote` in `src/pane/workbook-tab.ts`; `=PLSFIX.CAGR` showing a number Excel cannot print, or `#VALUE!` on values that look fine -> the finite check then `requirePositive` in `src/functions/index.ts`, then `cagr` in `src/chartmath.ts` (log space past the overflow); EVERY `=PLSFIX.*` answering #VALUE! on a desktop -> the `functions.js` script tag in `taskpane.html` (the shared runtime page must associate the functions itself); a workbook's saved `=PLSFIX.*` cell stuck on #NAME? -> the stored form must be `_xldudf_PLSFIX_<NAME>(...)`; a hygiene tool answering office.js's own string -> reproduce it in `test/stress.hygiene.pane.integration.test.ts` before touching the adapter; "nothing was changed" over a sheet that WAS partly painted -> a multi-area writer that ends on `syncWrite` without passing its area count (`protectedNote(stage, areas)` in `src/excel/protection.ts`); pls,fix Undo offering a write Excel refused -> a writer whose last sync is a bare `context.sync()` instead of `syncWrite`/`paintSync` (only those commit or discard the pending capture, `src/excel/undo.ts`); a double press drawing two charts -> `serialised` in `src/excel/chart-blocks.ts`; "X is now sheet N" naming a tab that is not there -> `visibleRank` in `src/excel/workbook.ts` (the receipt counts VISIBLE sheets since v2.8.20; `worksheet.position` counts hidden ones too). Every client of the hosted relay sharing one rate-limit bucket (the journal says so at startup) -> `MODELIS_TRUSTED_PROXY` in the host's unit (template `deploy/plsfix.service`), then `client_key` in `server/src/limits.rs`; a push answers 429 after a handful of requests -> the byte budget, `charge_bytes` in `server/src/relay_gates.rs`; a push answers 408/400 with nothing wrong in the pane -> the body stalled past `DEFAULT_BODY_DEADLINE_SECS`, `routes()` in `server/src/relay.rs`; pushes queue while the relay looks idle -> the permit arithmetic (`a_minute_of_stalled_writes_cannot_hold_every_permit`), then `inflight_writes()`; a link GET slow while a big push is in flight -> the concurrency layer must sit on `put(...).delete(...)`, not on the links `Router`; the store holds more than `MODELIS_MAX_BYTES` -> `room_locked` inside the write's lock, `server/src/store_room.rs`; `/version` counts stale or missing a fresh push -> `CountsCache` (30 s) and the sweeper's `counts.clear()`; `/healthz` slow while one query runs -> a store call that skipped `blocking::store_call`; a deck re-downloading a picture or a shared cache holding a sealed blob -> `cache_control` in `server/src/lib.rs`. A linked object the modeller deleted or ungrouped reported as `ItemNotFound: no shape` -> `missingShapeError` in `src/ppt/missing-shape.ts`; a rebuilt table half formatted while its row reads up to date -> the `beforeLast` tag in `refreshTable` and the tag `recreate` writes, `src/ppt/tables.ts`; a table link spending a round trip per eight cells for ever -> `requireTableSize`; "Selected 0 similar objects." with the selection cleared -> the `ids.includes(source.id)` guard in `src/ppt/object-tools.ts`; a pane button live again while a batch runs, two flows at once, or a ribbon press during a batch reporting "PowerPoint stopped answering" -> the `busyMessage` latch in `src/ui/guard.ts` and the `run` dep `src/ppt/main.ts` hands `registerCommands`; a repaint, a reinsertion or a tag write behaving oddly -> `src/ppt/refresh.ts`; a relay status line in a toast ("relay GET /api/links: 429") -> `relayReason` in `src/link/relay-reason.ts`, then its two call sites in `src/ppt/links.ts`; "Paste latest linked" landing a second copy -> the pasted set in `src/ppt/inbox-queue.ts` (the relay row survived a refused delete); PowerPoint for Mac dies right after a chart insert -> `groupTier` in `src/ppt/chart-draw.ts` (the Mac groups in sub-groups of six, `GROUP_TIER_MAC`, then those sub-groups again until at most six members remain; one flat addGroup of 19+ killed 16.107 on 13.09). Added 23.09 (slice H, click-through harness): a button dead over a CONNECTED fake host (not just with no host at all, which `ux:sweep` already covers) -> `test/clickthrough.excel.integration.test.ts` / `test/clickthrough.ppt.integration.test.ts`, KNOWN_DEFECTS at the top of whichever file names what was found; the Excel Links tab's own buttons (export-*, push-*, generate-key and the rest) answer nothing at all with no Excel connected at all, by construction - `src/pane/links-tab.ts`'s listeners are wired only inside `connectExcel()`, itself only called once `excelConnected` - so `scripts/ux/sweep-probe.js`'s id-wired button pass excludes everything under `#view-links` on the Excel pane alone (PowerPoint's own `#view-links` buttons are wired unconditionally in `src/ppt/main.ts` and stay covered); a control proven dead only with no host at all belongs to the vitest suite instead, which boots a connected fake and holds it to a real reaction. a destructive button acts on its first press, or a second arming shows no "Click again to confirm" -> `src/ui/confirm.ts` (the `<strong>` is looked up per paint) and that button's wiring (`data-confirm` in `wireControls`, `wireConfirm` in `links-tab.ts`, `CONFIRM_BUTTON_IDS` in `src/ppt/main.ts`); a cycle receipt names the wrong step -> `src/cycle-labels.ts` must match the cycle table's order; Reveal stuck after an action -> `setRuleButtonsSync` in `src/main.ts`; a Links-tab button dead without Excel -> the `!excelConnected` branch in `src/main.ts`; the sweep calls a button dead although it toasts -> the toast sentinel in `scripts/ux/sweep-probe.js` (a repeated sentence is byte-identical without it); a click-through suite says the latch never released -> `settle()` there drains while every tab is disabled, bounded. a paste does nothing -> `bundleFromPaste` (`src/ui/clipboard-links.ts`) and the Inbox tab's paste box (`#paste-links`, wired only in local mode); a copy never lands -> `beginClipboardWrite` / the Copy for PowerPoint bar (`#copy-ready`, `retryCopy` in `src/pane/links-transport.ts`); Not pasted yet on every row -> the transport mode (`src/ppt/transport.ts` mode(), the Settings select; check Excel and PowerPoint agree on local vs relay).
